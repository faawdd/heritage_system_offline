# 🎯 权限级别权限控制和文物点搜索筛选功能说明

## 功能1：权限级别的巡查员字段控制

### 实现原理

系统根据用户所属的用户组来决定巡查员字段的可编辑性：

| 用户组 | 巡查员字段 | 说明 |
|--------|----------|------|
| **文物看护员** | 🔒 只读/自动锁定 | 看护员添加的记录，巡查员自动锁定为自己，无法修改 |
| **管理员/其他** | 📝 可编辑 | 可以自由选择任何看护员作为巡查员 |

### 工作流程

#### 场景1：看护员用户添加巡查记录
```
看护员（如阿力木）登录系统
    ↓
访问 /mobile/add/ 手机页面
    ↓
检测到用户属于"文物看护员"组
    ↓
①前端：隐藏 inspector 字段，显示"（看护员，巡查员自动锁定）"
②后端：强制设置 inspector=request.user，忽略任何客户端值
    ↓
提交后，记录中的巡查员自动为"阿力木" ✅
```

#### 场景2：管理员用户添加巡查记录
```
管理员登录系统
    ↓
访问 /mobile/add/ 或 /admin/inspection/add/
    ↓
检测到用户不属于"文物看护员"组
    ↓
①前端：显示看护员选择下拉框（列出所有看护员）
②后端：允许 inspector 字段由客户端指定
    ↓
提交时，可以选择任意看护员
    ↓
记录中的巡查员为所选用户 ✅
```

### 代码实现

#### 1️⃣ 后端检查（core/views.py）

```python
# 检查用户是否属于"文物看护员"用户组
is_inspector = request.user.groups.filter(name='文物看护员').exists()

if request.method == 'POST':
    if is_inspector:
        # 看护员用户：强制使用当前登录用户
        inspector = request.user
    else:
        # 其他用户：允许指定巡查员
        inspector_id = data.get('inspector')
        if inspector_id:
            inspector = User.objects.get(id=inspector_id)
        else:
            inspector = request.user
```

#### 2️⃣ 前端条件显示（templates/admin/inspection_mobile.html）

```html
<!-- 只有非看护员用户才看到这个字段 -->
{% if not is_inspector %}
<div class="form-group">
    <label>巡查员 <span class="required">*</span></label>
    <select name="inspector">
        <option value="">-- 默认为当前用户 --</option>
        {% for inspector in inspectors %}
            <option value="{{ inspector.id }}">
                {{ inspector.first_name|default:inspector.username }}
            </option>
        {% endfor %}
    </select>
</div>
{% endif %}
```

#### 3️⃣ 管理后台权限控制（core/admin.py）

```python
def get_readonly_fields(self, request, obj=None):
    """根据用户权限动态设置只读字段"""
    readonly = list(self.readonly_fields)
    
    is_inspector = request.user.groups.filter(name='文物看护员').exists()
    
    if is_inspector:
        # 看护员用户：inspector 字段只读
        readonly.append('inspector')
    
    return readonly
```

### 验证步骤

#### 测试1：看护员添加记录

```bash
# 1. 用看护员账户登录
用户名: 13565713009
密码: 初始密码（需要修改）

# 2. 访问手机端页面
http://localhost:8000/mobile/add/

# 预期：
✓ 不显示"巡查员"字段
✓ 显示"（看护员，巡查员自动锁定）"
✓ 填写表单并提交
✓ 记录的巡查员自动为当前登录用户
```

#### 测试2：管理员添加记录

```bash
# 1. 用管理员账户登录
用户名: admin
密码: 你的管理员密码

# 2. 访问管理后台
http://localhost:8000/admin/inspection/add/

# 预期：
✓ "巡查员"字段可编辑（不是灰色）
✓ 可以选择任意看护员
✓ 或者访问手机端 /mobile/add/?force_desktop=1
✓ 能看到看护员选择下拉框
```

---

## 功能2：文物点搜索和筛选

### 实现特性

✨ **搜索功能**
- 实时搜索文物点名称
- 输入即可筛选

✨ **分类筛选**
- 按文物分类筛选：古建筑、古遗址、古墓葬、石刻、其他

✨ **等级筛选**
- 按保护等级筛选：国家级、自治区级、县级

✨ **实时统计**
- 显示当前筛选结果的文物点数量

### 用户界面

```
┌─────────────────────────────────────┐
│  选择文物点                         │
│  🔍 搜索文物点名称...     (输入框)   │
│                                    │
│  [全部分类▼] [全部等级▼]  (筛选)   │
│                                    │
│  [-- 请选择文物点 --▼]   (下拉框)   │
│  共 622 个文物点                   │
└─────────────────────────────────────┘
```

### 工作流程

```
用户输入搜索词 → 输入事件触发 updateSiteSelect()
    ↓
遍历后端传回的文物点数据
    ↓
按搜索词、分类、等级进行过滤
    ↓
动态更新下拉框选项
    ↓
更新底部统计数字
```

### 代码实现

#### 文物点数据准备（core/views.py）

```python
# GET 请求：返回手机优化的表单页面
sites_data = list(HeritageSite.objects.all().values('id', 'name', 'category', 'level'))

context = {
    'sites_json': json.dumps(sites_data),  # JSON格式供前端搜索筛选
    'sites': sites_data,  # 也提供列表格式
    'is_inspector': is_inspector,
}
```

#### 前端搜索筛选逻辑（templates/admin/inspection_mobile.html）

```javascript
// 更新文物点选择框
function updateSiteSelect() {
    const searchText = siteSearch.value.toLowerCase();
    const category = categoryFilter.value;
    const level = levelFilter.value;

    // 过滤文物点
    const filteredSites = sitesData.filter(site => {
        const matchName = site.name.toLowerCase().includes(searchText);
        const matchCategory = !category || site.category === category;
        const matchLevel = !level || site.level === level;
        return matchName && matchCategory && matchLevel;
    });

    // 重建选择框
    siteSelect.innerHTML = '<option value="">-- 请选择 --</option>';
    filteredSites.forEach(site => {
        const option = document.createElement('option');
        option.value = site.id;
        option.textContent = `${site.name} (${site.category}) [${site.level}]`;
        siteSelect.appendChild(option);
    });

    // 更新计数
    siteCount.textContent = filteredSites.length;
}

// 绑定事件
siteSearch.addEventListener('input', updateSiteSelect);
categoryFilter.addEventListener('change', updateSiteSelect);
levelFilter.addEventListener('change', updateSiteSelect);
```

### 使用示例

#### 例1：快速找到坎儿井相关文物

```
点击搜索框，输入"坎儿井"
    ↓
列表立即筛选出所有包含"坎儿井"的文物点
    ↓
显示"共 XX 个文物点"
    ↓
从下拉框选择想要的坎儿井点
```

#### 例2：查找国家级建筑

```
点击"全部等级"下拉框，选择"国家级"
    ↓
列表只显示国家级文物
    ↓
再输入名字或选择分类进一步筛选
```

#### 例3：查找古遗址中的特定文物

```
点击"全部分类"，选择"古遗址"
    ↓
搜索框输入"吐峪沟"
    ↓
立即显示"吐峪沟遗址"
```

### 搜索筛选的特点

| 特性 | 说明 |
|-----|------|
| 🚀 实时性 | 输入即刻反映结果，无需点击搜索按钮 |
| 🔄 组合筛选 | 支持多条件组合（如：古建筑 + 国家级） |
| 📊 动态计数 | 随时显示当前筛选范围内的文物点数量 |
| 💡 完整信息 | 下拉框中显示名称、分类、等级，便于选择 |
| 📱 手机优化 | 减少滚动，快速定位想要的文物点 |

---

## 集成效果

### 综合场景：看护员现场使用

```
1. 看护员打开手机，访问 /mobile/add/
   ✓ 自动登录检测，显示"看护员，巡查员自动锁定"

2. 搜索要巡查的文物点
   输入"观音阁"或"多来提"，快速找到
   
3. 如果有很多结果，可以
   - 按分类筛选（古建筑、古遗址等）
   - 按等级筛选（国家级、区级等）
   - 组合使用快速定位
   
4. 选择某个文物点
   ✓ 巡查员自动锁定为当前用户
   
5. 填写其他信息
   - 时间、状态、问题描述、拍照
   
6. 提交
   ✓ 返回消息："巡查记录已保存！巡查员：[自动填充用户名]"
```

### 综合场景：管理员审核使用

```
1. 管理员访问 /admin/inspection/add/
   ✓ 看到"巡查员"字段可编辑

2. 可以代表某个看护员添加记录
   - 选择文物点（支持搜索筛选）
   - 选择巡查员（哪个看护员执行的）
   - 填写其他信息
   
3. 提交
   ✓ 记录中的巡查员为所选用户
   ✓ 方便管理员导入历史数据或补录遗漏记录
```

---

## 修改的文件清单

| 文件 | 修改内容 | 影响 |
|-----|--------|------|
| `core/views.py` | 添加用户组检查，根据权限决定 inspector 处理方式 | 手机端权限控制 |
| `core/admin.py` | 添加 `get_readonly_fields()` 动态设置只读字段 | 后台权限控制 |
| `templates/admin/inspection_mobile.html` | 添加搜索筛选UI，条件化显示 inspector 字段，AJAX提交 | 前端权限控制和搜索功能 |

---

## 系统配置

### 用户组信息

```
用户组名称：文物看护员
权限包括：
  ✓ 添加巡查记录
  ✓ 查看巡查记录
  ✓ 编辑自己的巡查记录
  ✓ 删除自己的巡查记录（可选）

成员：18名看护员账户
  - 13565713009（阿力木·艾尼）
  - 13565713010（托乎提·买买提）
  - ... 更多账户
```

### 数据库设置

```
文物点总数：622个（来自导入的数据）
支持的分类：
  - 古建筑
  - 古遗址
  - 古墓葬
  - 石刻
  - 其他
  
保护等级：
  - 全国重点（国家级）
  - 自治区级（自治区级）
  - 县级（县级）
```

---

## FAQ

### Q1：看护员能否修改自己的巡查员字段？

**A:** 不能。无论在手机端还是后台，看护员的巡查员字段都是只读的（锁定为自己）。这是为了确保数据准确性和防止冒充。

### Q2：管理员如何添加看护员代做的巡查记录？

**A:** 管理员可以：
1. 访问 `/admin/inspection/add/`
2. 选择文物点（支持搜索筛选）
3. 在"巡查员"字段选择实际执行人的账户
4. 填写其他信息后提交

### Q3：搜索筛选是否支持高级查询（如：正则表达式）？

**A:** 不支持。当前实现是简单的包含匹配（contains）。如需高级查询，可在筛选后结果中再用浏览器Ctrl+F进行二次过滤。

### Q4：如何批量导入文物点数据？

**A:** 系统已包含622个文物点数据。如需添加或修改，可以：
1. 访问 `/admin/heritage/` 管理页面
2. 使用"导入"功能批量上传
3. 支持的格式：CSV、Excel等（需要通过 django-import-export 配置）

### Q5：看护员能否看到其他看护员的巡查记录？

**A:** 不能。在后台查询时，系统自动过滤只显示自己的记录（通过 `get_queryset()` 方法实现）。

---

## 技术细节

### 权限检查流程

```python
# 在中间件或视图中检查
is_inspector = request.user.groups.filter(name='文物看护员').exists()

if is_inspector:
    # A. 看护员逻辑
    readonly_inspector = True
else:
    # B. 管理员逻辑
    readonly_inspector = False
```

### 文物点数据流

```
Django后端
    ↓
HeritageSite.objects.all().values('id', 'name', 'category', 'level')
    ↓
JSON序列化: sites_json = json.dumps(...)
    ↓
模板中: {{ sites_json|safe }}
    ↓
JavaScript前端
    ↓
JSON.parse() 解析为数组
    ↓
实时搜索和筛选
```

---

**测试清单**

- [ ] 看护员添加记录，巡查员自动锁定
- [ ] 管理员添加记录，可选择巡查员
- [ ] 搜索功能正常
- [ ] 分类筛选正常
- [ ] 等级筛选正常
- [ ] 组合筛选正常
- [ ] 统计数字准确
- [ ] 表单提交成功
- [ ] AJAX错误处理正确

**最后更新：2026-03-03**
