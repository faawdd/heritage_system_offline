# 基层文物综合管理平台 - 系统管理模块实施方案（阶段1-5）

## 阶段1：现状分析结论（已完成）

### 1. 用户/角色/权限现状
- 用户模型：当前使用 `django.contrib.auth.models.User`，无自定义 User 替换。
- 角色模型：当前使用 `django.contrib.auth.models.Group`，并已有四层角色体系（超级管理员、管理员、管理员用户组、文物看护员）。
- 权限模型：当前使用 `django.contrib.auth.models.Permission`，并通过管理命令初始化角色权限映射。
- 结论：满足“禁止重构用户/角色/权限体系”的要求，可直接复用。

### 2. 认证与鉴权现状
- 已安装 `djangorestframework-simplejwt`，DRF 已配置 JWTAuthentication。
- 前端当前缺少统一 token 登录/刷新链路，主要依赖同域会话调试。
- 结论：后端新增 `/api/v1/system/login` 等 JWT 接口，前端新增 token 拦截与自动刷新。

### 3. 已有可复用日志能力
- 已有 `UserManagementAudit`（用户/用户组操作日志）但仅覆盖后台管理部分动作。
- 已有 Django `LogEntry` 使用，但不满足完整 API 级操作日志要求。
- 结论：新增 `LoginLog` + `OperationLog` 统一日志模型；旧日志不删除，保留兼容。

### 4. 前端现状
- 菜单与路由写死在布局中。
- 仅有项目管理 store，系统管理模块为空。
- 结论：新增 system 模块后，优先实现“数据库菜单驱动左侧导航”的兼容方案（保留旧静态兜底）。

---

## 阶段2：系统管理模块总体架构设计

### 2.1 后端分层
采用独立 Django app：`system/`

- `system/models.py`：系统管理领域模型
- `system/serializers.py`：序列化层
- `system/services/`：业务服务层（核心逻辑）
- `system/permissions.py`：系统管理权限校验
- `system/views.py`：轻量 API 控制层
- `system/urls.py`：统一路由

调用链：

View -> Service -> Model

### 2.2 权限策略
- 认证：JWT（SimpleJWT）
- 授权：复用 `User/Group/Permission`
- 管理端接口默认要求管理权限（复用现有 `is_management_admin`）
- 细粒度接口可追加 Django permission codename 校验

### 2.3 前端分层

- `frontend/src/api/system/`：系统管理 API
- `frontend/src/stores/system/`：用户会话、菜单、字典缓存
- `frontend/src/views/system/`：页面层
- `frontend/src/components/system/`：复用组件

---

## 阶段3：数据库影响分析（Zero Destruction）

### 3.1 新增模型（仅新增，不替换）
1. `Menu`
- 用途：目录/菜单/按钮，支持路由、组件、图标、排序、隐藏、外链、KeepAlive、权限标识。

2. `LoginLog`
- 用途：登录成功/失败审计。

3. `OperationLog`
- 用途：接口级操作日志（自动采集）。

4. `DictionaryType` + `DictionaryItem`
- 用途：统一数据字典。

5. `SystemConfig`
- 用途：键值化系统参数（系统名、地图中心、上传大小、FastAPI地址等）。

### 3.2 复用与扩展模型
- 复用 `User`、`Group`、`Permission`。
- 复用 `UserProfile`，可做“可选扩展字段”（如部门）但不改用户主表。

### 3.3 迁移策略
- 全部新增迁移，不改已有主键、不删已有字段。
- 初始数据迁移包含：默认系统菜单、默认字典类型、默认系统配置。

---

## 阶段4：API 设计（/api/v1/system）

### 4.1 认证与会话
- `POST /api/v1/system/login`
- `POST /api/v1/system/refresh`
- `POST /api/v1/system/logout`
- `GET /api/v1/system/profile`
- `PATCH /api/v1/system/profile`
- `POST /api/v1/system/profile/change-password`

登录返回：
- `access_token`
- `refresh_token`
- `user`
- `roles`
- `permissions`
- `menus`

### 4.2 用户管理
- `GET /api/v1/system/users`
- `POST /api/v1/system/users`
- `GET /api/v1/system/users/{id}`
- `PATCH /api/v1/system/users/{id}`
- `DELETE /api/v1/system/users/{id}`
- `POST /api/v1/system/users/{id}/reset-password`
- `POST /api/v1/system/users/{id}/enable`
- `POST /api/v1/system/users/{id}/disable`
- `POST /api/v1/system/users/batch-delete`

### 4.3 角色管理（Group）
- `GET /api/v1/system/roles`
- `POST /api/v1/system/roles`
- `PATCH /api/v1/system/roles/{id}`
- `DELETE /api/v1/system/roles/{id}`
- `GET /api/v1/system/roles/{id}/permissions`
- `PUT /api/v1/system/roles/{id}/permissions`
- `GET /api/v1/system/roles/{id}/menus`
- `PUT /api/v1/system/roles/{id}/menus`

### 4.4 权限管理（Permission）
- `GET /api/v1/system/permissions`
- `GET /api/v1/system/permissions/tree`

### 4.5 菜单管理
- `GET /api/v1/system/menus`
- `POST /api/v1/system/menus`
- `PATCH /api/v1/system/menus/{id}`
- `DELETE /api/v1/system/menus/{id}`
- `GET /api/v1/system/menus/tree`

### 4.6 日志与配置
- `GET /api/v1/system/login-log`
- `GET /api/v1/system/operation-log`
- `GET /api/v1/system/login-log/export`
- `GET /api/v1/system/operation-log/export`
- `GET /api/v1/system/config`
- `PUT /api/v1/system/config`

### 4.7 数据字典
- `GET /api/v1/system/dict/types`
- `GET /api/v1/system/dict/items`
- `POST /api/v1/system/dict/types`
- `POST /api/v1/system/dict/items`
- `PATCH /api/v1/system/dict/types/{id}`
- `PATCH /api/v1/system/dict/items/{id}`
- `DELETE /api/v1/system/dict/types/{id}`
- `DELETE /api/v1/system/dict/items/{id}`

### 4.8 系统管理 Dashboard
- `GET /api/v1/system/dashboard`

---

## 阶段5：Vue 页面设计

## 5.1 一级菜单
- 系统管理

### 5.2 子页面
- 用户管理
- 角色管理
- 权限管理
- 菜单管理
- 登录日志
- 操作日志
- 系统配置
- 数据字典
- 个人中心
- 系统管理首页（Dashboard）

### 5.3 页面拆分建议
- `views/system/dashboard/SystemDashboardView.vue`
- `views/system/user/UserListView.vue`
- `views/system/role/RoleListView.vue`
- `views/system/permission/PermissionView.vue`
- `views/system/menu/MenuListView.vue`
- `views/system/log/LoginLogView.vue`
- `views/system/log/OperationLogView.vue`
- `views/system/config/SystemConfigView.vue`
- `views/system/dict/DictionaryView.vue`
- `views/system/profile/ProfileView.vue`

配套复用组件：
- `components/system/UserFormDrawer.vue`
- `components/system/RolePermissionDialog.vue`
- `components/system/MenuTreeEditor.vue`
- `components/system/DictItemEditor.vue`

---

## 第六阶段启动顺序（执行中）

优先落地顺序：
1. 后端基础模型 + 路由 + 认证接口 + 用户/角色/权限/菜单查询接口
2. 前端登录态 store + token 拦截器 + 系统管理路由骨架
3. 用户管理、角色管理、菜单管理三大核心页面
4. 日志、字典、配置、个人中心补齐
5. 菜单动态化（数据库驱动）替换写死菜单
