# 用户管理与权限控制指南

**最后更新**: 2026年3月5日  
**版本**: 1.0  
**维护**: 文物数字化管理团队

---

## 📋 目录

1. [系统架构](#系统架构)
2. [用户组划分](#用户组划分)
3. [权限配置](#权限配置)
4. [用户管理界面](#用户管理界面)
5. [初始化与维护](#初始化与维护)
6. [开发者指南](#开发者指南)
7. [故障排查](#故障排查)

---

## 🏗️ 系统架构

系统采用 Django 内置的权限系统，结合自定义的用户组和权限装饰器，实现灵活的权限控制。

### 核心组件

```
┌─────────────────────────────────────────────┐
│          用户权限管理系统                      │
├─────────────────────────────────────────────┤
│ • Django User 模型（用户基本信息）             │
│ • Django Group 模型（用户组定义）             │
│ • Django Permission 模型（权限定义）          │
│ • UserProfile 模型（用户扩展信息）            │
├─────────────────────────────────────────────┤
│ 权限应用层                                   │
│ • permission_decorators.py（权限装饰器）     │
│ • 中间件权限检查                              │
│ • 视图级权限控制                              │
├─────────────────────────────────────────────┤
│ 管理界面                                     │
│ • CustomUserAdmin（用户管理）                │
│ • CustomGroupAdmin（用户组管理）             │
│ • UserProfileAdmin（登录状态管理）           │
└─────────────────────────────────────────────┘
```

---

## 👥 用户组划分

系统定义了三个用户组，权限逐级递减：

### 1️⃣ 超级管理员 (👑 wenwu1)

| 字段 | 值 |
|------|-----|
| **唯一账号** | `wenwu1` |
| **权限级别** | 最高 |
| **权限数量** | 所有权限 |
| **可进行操作** | 所有操作 |

**权限清单**:
- ✅ 文物档案管理（增删改查）
- ✅ 巡查记录管理（所有操作）
- ✅ 项目管理（全部工作流）
- ✅ 坐标管理（全部操作）
- ✅ 用户管理（创建、删除、权限调整）
- ✅ 用户组管理
- ✅ 系统设置
- ✅ 数据导入导出

**特殊设置**:
- 自动设为 Django 超级用户（`is_superuser=True`）
- 自动设为员工（`is_staff=True`）
- 不可被降级或删除（需要数据库直接修改）

---

### 2️⃣ 管理员

| 字段 | 值 |
|------|-----|
| **权限级别** | 高 |
| **权限数量** | 约 20 个 |
| **可进行操作** | 大部分管理操作 |
| **限制** | 不能删除超级管理员，不能修改系统设置 |

**权限清单**:
- ✅ 文物档案查看
- ✅ 巡查记录管理（全部）
- ✅ 项目管理（全部）
- ✅ 坐标管理（全部）
- ✅ 用户管理（创建、编辑）
- ✅ 用户组查看和编辑
- ✅ 数据导入导出

**使用场景**:
- 一线项目经理
- 数据维护人员
- 巡查协调员

---

### 3️⃣ 文物看护员 (👷)

| 字段 | 值 |
|------|-----|
| **权限级别** | 低 |
| **权限数量** | 约 5 个 |
| **可进行操作** | 仅巡查上报 |

**权限清单**:
- ✅ 查看文物档案（只读）
- ✅ 提交巡查记录（新建+编辑）
- ✅ 查看自己的巡查记录

**功能限制**:
- ❌ 不能编辑文物档案
- ❌ 不能删除巡查记录
- ❌ 不能查看其他人的巡查记录
- ❌ 不能访问项目管理模块

**使用场景**:
- 实地巡查人员
- 基层看护员
- 信息采集员

---

## 🔐 权限配置

### 权限关键词说明

系统权限基于 Django 权限系统，格式为: `<操作>_<模型>`

| 操作 | 说明 | 示例 |
|------|------|------|
| `add_` | 新建 | `add_inspectionrecord` → 新建巡查记录 |
| `change_` | 修改 | `change_inspectionrecord` → 编辑巡查记录 |
| `view_` | 查看 | `view_inspectionrecord` → 查看巡查记录 |
| `delete_` | 删除 | `delete_inspectionrecord` → 删除巡查记录 |

### 权限模型映射

```
HeritageSite        → 文物档案
InspectionRecord    → 巡查记录
ProjectAudit        → 项目管理
Coordinate          → 坐标管理
User                → 用户账户
Group               → 用户组
UserProfile         → 用户配置
```

---

## 👨‍💼 用户管理界面

### 访问位置

**后台 → 认证和授权 → 用户**

```
后台首页
└─ 认证和授权
   ├─ 用户 ← 在这里管理用户
   ├─ 用户组 ← 在这里管理用户组
   └─ 密码修改记录 ← 查看登录状态
```

### 用户管理操作

#### 添加新用户

1. 点击"添加用户"按钮
2. 输入用户名和密码
3. 填写可选信息（姓名、邮箱）
4. **在"权限与用户组"中选择用户组**：
   - 超级管理员 （仅限超级管理员操作）
   - 管理员
   - 文物看护员
5. 勾选"激活"使用户账户生效
6. 点击"保存"

#### 编辑用户权限（实时生效）

1. 点击要修改的用户
2. 滚动到"权限与用户组"部分
3. **修改"用户组"选择**（可多选）
4. 点击"保存"

✅ **修改立即生效**，无需重启服务器

#### 删除用户

1. 进入用户列表
2. 勾选要删除的用户（不包括超级管理员）
3. 在下拉菜单中选择"删除"
4. 确认删除

⚠️ **注意**: 删除用户会同时删除其关联的巡查记录，请谨慎操作

### 用户组管理操作

#### 查看用户组权限

1. 进入 **用户组** 管理页面
2. 点击要查看的用户组（如 "管理员"）
3. 查看"权限配置"部分显示的所有权限

#### 调整用户组权限（实时生效）

1. 进入用户组编辑页面
2. 在"权限配置"部分：
   - **左侧框**: 当前用户组没有的权限
   - **右侧框**: 当前用户组拥有的权限
3. 选择权限并使用箭头按钮转移
4. 点击"保存"

✅ **权限修改立即生效**，所有该用户组的成员权限自动更新

#### 权限列表解读

```
add_heritagesite          → 新建文物档案
change_heritagesite       → 修改文物档案
view_heritagesite         → 查看文物档案
add_inspectionrecord      → 新建巡查记录
change_inspectionrecord   → 修改巡查记录
view_inspectionrecord     → 查看巡查记录
delete_inspectionrecord   → 删除巡查记录
...（更多权限）
```

---

## 🔧 初始化与维护

### 初始化用户组（首次安装）

系统第一次运行时需要初始化用户组和权限。执行以下命令：

```bash
# 进入项目目录
cd /path/to/heritage_system

# 运行初始化命令
python manage.py init_user_groups
```

**预期输出**:
```
============================================================
正在初始化用户组和权限配置...

✓ 创建用户组: 超级管理员
  描述: 系统最高权限，具有所有功能访问权限
  权限: 所有权限 (XX 个)

✓ 创建用户组: 管理员
  描述: 系统管理员，拥有大部分管理权限
  权限: 20 个

✓ 创建用户组: 文物看护员
  描述: 巡查人员，只能访问文物点巡查上报功能
  权限: 5 个

✓ 用户 wenwu1 已设置为超级管理员
  权限: 最高级别

============================================================
✓ 用户组和权限初始化完成！
```

### 重置用户组（谨慎使用）

如果需要完全重置所有用户组和权限配置：

```bash
python manage.py init_user_groups --reset
```

⚠️ **警告**: 此操作会：
- 删除所有现有用户组
- 清除所有用户的组成员关系
- 需要重新为用户分配权限

---

### 设置超级管理员

如果 `wenwu1` 账户不存在，需要手动创建：

```bash
# 创建超级用户（交互式）
python manage.py createsuperuser

# 按提示输入：
# Username: wenwu1
# Email: admin@example.com
# Password: (输入密码)

# 然后运行初始化命令
python manage.py init_user_groups
```

或者使用 Django shell：

```python
python manage.py shell

from django.contrib.auth.models import User, Group

# 创建用户
user = User.objects.create_user(
    username='wenwu1',
    email='admin@example.com',
    password='初始密码',  # 重要：首次登录后应更改
    is_staff=True,
    is_superuser=True,
    first_name='文物'
)

# 分配超级管理员组
group = Group.objects.get(name='超级管理员')
user.groups.add(group)

exit()
```

---

## 👨‍💻 开发者指南

### 在代码中检查权限

#### 1. 使用装饰器（推荐）

```python
from core.permission_decorators import (
    group_required,
    admin_required,
    inspector_required,
    super_admin_required
)

# 要求特定用户组
@group_required('管理员', '超级管理员')
def my_admin_view(request):
    return HttpResponse('管理员视图')

# 要求管理员或超级管理员
@admin_required
def admin_only_view(request):
    return HttpResponse('仅管理员')

# 要求看护员权限
@inspector_required
def inspection_view(request):
    return HttpResponse('检查记录视图')

# 要求超级管理员
@super_admin_required
def super_admin_view(request):
    return HttpResponse('超级管理员视图')
```

#### 2. 在模板中检查权限

```html
<!-- Jinja2 模板 -->

<!-- 检查用户组 -->
{% if user.groups.all|length %}
    <p>用户组: {% for group in user.groups.all %}{{ group.name }}{% endfor %}</p>
{% endif %}

<!-- 检查是否为超级管理员 -->
{% if user.is_superuser %}
    <a href="/admin/users/">用户管理</a>
{% endif %}

<!-- 检查特定权限 -->
{% if perms.core.add_inspectionrecord %}
    <button>新建巡查记录</button>
{% endif %}

<!-- 检查特定用户组 -->
{% if 'admin' in user.groups.values_list 'name' %}
    <p>你是管理员</p>
{% endif %}
```

#### 3. 在 Python 代码中检查权限

```python
from core.permission_decorators import (
    has_group,
    is_super_admin,
    is_admin,
    is_inspector
)

# 检查用户组
if has_group(request.user, '管理员'):
    print("用户是管理员或超级管理员")

# 检查特定角色
if is_super_admin(request.user):
    print("超级管理员")

if is_admin(request.user):
    print("管理员或超级管理员")

if is_inspector(request.user):
    print("文物看护员")

# 直接使用 Django 权限
if request.user.has_perm('core.add_inspectionrecord'):
    print("有权限新建巡查记录")

# 检查用户组成员
if request.user.groups.filter(name='文物看护员').exists():
    print("是看护员")
```

#### 4. 在 Admin 类中的权限控制

```python
class MyModelAdmin(admin.ModelAdmin):
    def get_queryset(self, request):
        """根据权限过滤查询集"""
        qs = super().get_queryset(request)
        
        if request.user.is_superuser:
            return qs  # 超级管理员看全部
        
        if request.user.groups.filter(name='文物看护员').exists():
            return qs.filter(inspector=request.user)  # 看护员只看自己的
        
        return qs
    
    def save_model(self, request, obj, form, change):
        """修改保存逻辑"""
        if not change:  # 新建时
            if request.user.groups.filter(name='文物看护员').exists():
                obj.inspector = request.user  # 自动设置为当前用户
        super().save_model(request, obj, form, change)
```

---

### 为新模型配置权限

如果添加了新的 Django 模型，权限会自动生成。但要在用户组中配置：

```python
# 1. 创建权限（Django 自动完成）
# 执行 python manage.py migrate

# 2. 在初始化命令中添加新权限
# 修改 core/management/commands/init_user_groups.py

permissions = [
    'add_mynewmodel',
    'change_mynewmodel',
    'view_mynewmodel',
    'delete_mynewmodel',  # 可选
]

# 3. 重新运行初始化
python manage.py init_user_groups --reset
```

---

## 🔍 故障排查

### 问题1: 用户登录后无任何权限

**症状**: 用户可以登录，但看不到任何功能

**原因**: 用户未分配用户组

**解决**:
1. 进入后台 → 用户 → 编辑用户
2. 在"权限与用户组"中选择合适的用户组
3. 点击保存

### 问题2: 新创建的权限不会生效

**症状**: 修改了权限配置，但用户还是没有权限

**原因**: 权限缓存或用户组更新不完整

**解决**:
```bash
# 清除权限缓存
python manage.py clear_cache  # 如果有缓存系统

# 重新加载权限
python manage.py init_user_groups --reset

# 为用户重新分配组
```

### 问题3: wenwu1 无法执行某些操作

**症状**: 即使是 wenwu1，也被拒绝访问

**原因**: is_staff 或 is_superuser 标志未设置

**解决**:
```python
from django.contrib.auth.models import User

user = User.objects.get(username='wenwu1')
user.is_staff = True
user.is_superuser = True
user.save()
```

### 问题4: 权限修改后用户仍无法使用

**症状**: Django 缓存了权限信息

**原因**: 权限缓存未清除

**解决**:
1. 让用户重新登录
2. 清除服务器缓存
3. 重启 Django 开发服务器

---

## 📊 权限管理最佳实践

| 场景 | 推荐做法 |
|------|---------|
| **新员工入职** | 创建用户 → 分配合适用户组 → 通知修改密码 |
| **员工离职** | 从用户组中移除 → 禁用账户（取消激活）或删除 |
| **员工升职** | 在用户编辑界面中修改用户组即可 |
| **权限范围扩大** | 修改用户组权限配置（自动对所有成员生效） |
| **审计权限** | 进入用户组列表，查看每个组的成员和权限 |
| **紧急权限变更** | 直接修改用户组，无需重启服务器 |

---

## ✅ 检查清单

部署前确保：

- [ ] 执行了 `init_user_groups` 命令
- [ ] wenwu1 账户已创建并设为超级管理员
- [ ] 至少有一个管理员账户
- [ ] 所有看护员已分配到"文物看护员"用户组
- [ ] 权限配置已在生产环境中验证
- [ ] 用户已被告知初始密码并在首次登录时修改

---

## 📞 获取帮助

- **查看用户权限**: 后台 → 用户组 → 选择组 → 查看成员列表
- **调整权限**: 后台 → 用户组 → 编辑权限配置 → 保存
- **紧急访问**: 以 wenwu1 超级管理员身份登录
- **问题反馈**: 与开发团队联系

---

**最后更新**: 2026年3月5日  
**维护团队**: 鄯善县文物数字化管理项目组  
**版本**: 1.0 - 初始版本
