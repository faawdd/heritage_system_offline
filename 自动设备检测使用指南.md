# 🎯 自动设备检测与页面切换使用指南

## 功能概述

系统已实现**智能设备检测**，根据访问设备类型自动切换显示不同的界面版本：

| 设备类型 | 访问 URL | 自动跳转 | 使用场景 |
|---------|---------|--------|--------|
| **手机** | `/admin/` | ✅ 自动跳转到 `/mobile/add/` | 现场快速添加巡查记录 |
| **平板** | `/admin/` | ❌ 不跳转，显示桌面版 | 支持桌面版完整功能 |
| **电脑** | `/admin/` | ❌ 不跳转，显示桌面版 | 完整管理后台界面 |

---

## 工作原理

### 1️⃣ 后端检测（中间件自动化）
```
用户请求 → DeviceAutoRedirectMiddleware中间件
         → 检查User-Agent
         → 判断设备类型
         → 自动重定向或显示对应页面
```

**检测流程：**
- ✅ 识别 User-Agent 中的关键词（android, iphone, mobile等）
- ✅ 排除平板设备（iPad, 安卓平板等）
- ✅ 仅对手机设备执行重定向

**检测代码位置:** [`core/device_detector.py`](core/device_detector.py)

### 2️⃣ 前端检测（客户端提示）
```
首页加载 → 运行JavaScript检测
        → 检测到手机 → 显示"前往手机版"浮窗
        → 用户点击链接进入手机页面
```

**前端检测代码位置:** [`templates/admin/index_dashboard.html`](templates/admin/index_dashboard.html#L309-L322)

---

## 使用场景

### ✅ 场景1：看护员在现地用手机访问系统
```
1. 看护员打开手机浏览器
2. 访问 http://system.local/admin/
3. 系统自动检测到手机
4. 自动跳转到 /mobile/add/（添加巡查页面）
5. 看护员直接填写表单，快速添加记录
```

### ✅ 场景2：办公室管理员用电脑访问
```
1. 管理员打开电脑浏览器
2. 访问 http://system.local/admin/
3. 系统检测到桌面设备
4. 显示标准Django管理后台界面
5. 管理员可浏览统计数据、导出报表等
```

### ✅ 场景3：看护员想在桌面版查看管理界面
```
1. 看护员在手机上访问 /mobile/add/
2. 在页面右上角点击"🔗 桌面版"链接
3. 系统添加 force_desktop=1 参数
4. 跳转到标准管理后台显示完整界面
5. （仅此次访问强制桌面版，下次自动检测还原）
```

---

## 路由配置

### 自动跳转路由

| 入口 | 设备类型 | 目标 |
|-----|--------|------|
| `/admin/` | 手机 | `/mobile/add/` |
| `/admin/` | 电脑/平板 | 保持显示 `/admin/` |
| `/admin/home/` | 手机 | `/mobile/add/` |
| `/admin/home/` | 电脑/平板 | 保持显示 `/admin/home/` |
| `/mobile/add/` | 所有 | 保持显示 `/mobile/add/` |
| `/mobile/list/` | 所有 | 保持显示 `/mobile/list/` |

### 强制参数覆盖

在任何URL后添加 `?force_desktop=1` 可强制显示桌面版：

```
/admin/?force_desktop=1          → 强制显示管理后台（即使是手机）
/admin/home/?force_desktop=1     → 强制显示首页仪表板（即使是手机）
```

---

## 用户界面

### 首页自动检测提示
当手机用户访问 `/admin/home/` 时，会看到：

```
┌─────────────────────────────────────────────┐
│ 📱 检测到移动设备                            │
│ 建议使用手机端界面以获得更好体验              │
│                        [前往手机版] →        │
└─────────────────────────────────────────────┘
```

### 手机端返回桌面版
两个手机端页面右上角都有"🔗 桌面版"链接，点击可临时切换到桌面版显示。

---

## 配置文件

### 1. 中间件配置
**文件:** [`heritage_system/settings.py`](heritage_system/settings.py#L44-L53)

```python
MIDDLEWARE = [
    # ... 其他中间件 ...
    'core.middleware.DeviceAutoRedirectMiddleware',  # 新添加的自动重定向
]
```

### 2. 设备检测器模块
**文件:** [`core/device_detector.py`](core/device_detector.py)

包含两个核心函数：
```python
get_device_type(request)   # 返回 'mobile', 'tablet', 或 'desktop'
is_mobile_device(request)  # 返回布尔值 True/False
```

### 3. 中间件实现
**文件:** [`core/middleware.py`](core/middleware.py)

核心类：
```python
class DeviceAutoRedirectMiddleware:
    """根据设备类型自动重定向到对应页面"""
```

---

## 测试步骤

### 测试1：手机自动重定向
```bash
# 1. 使用Chrome开发者工具
# 2. 按 F12 打开开发者工具
# 3. 点击左上角设备工具栏图标 📱
# 4. 选择 iPhone 12 或其他手机型号
# 5. 访问 http://localhost:8000/admin/
# 预期：自动跳转到 /mobile/add/
```

### 测试2：强制桌面版显示
```bash
# 1. 在手机模式下访问
http://localhost:8000/admin/?force_desktop=1
# 预期：保持显示管理后台，不跳转
```

### 测试3：真实手机测试
```bash
# 1. 用真实手机访问
http://[电脑IP]:8000/admin/
# 例如：http://192.168.1.100:8000/admin/
# 预期：自动跳转到手机版 /mobile/add/
```

### 测试4：平板设备检测
```bash
# 1. 使用Chrome开发者工具
# 2. 选择 iPad 设备
# 3. 访问 http://localhost:8000/admin/
# 预期：显示桌面版管理后台（不跳转）
```

---

## 常见问题

### Q1: 为什么我用手机访问时没有自动跳转？
**A:** 检查以下几点：
1. ✅ 确保已登录（未登录不会自动检测）
2. ✅ 浏览器User-Agent包含"mobile"或类似关键词
3. ✅ 不是平板设备（iPad等会被排除）
4. ✅ 中间件已添加到settings.py

### Q2: 如何强制显示桌面版？
**A:** 在URL后添加参数：
```
/admin/?force_desktop=1
/mobile/add/?force_desktop=1
```

### Q3: 支持哪些手机设备？
**A:** 所有现代智能手机都支持，包括：
- 📱 iPhone（iOS）
- 🤖 安卓手机
- 📲 Windows Phone
- 其他Unix/Linux系统的手机

### Q4: 支持平板设备吗？
**A:** 平板会显示桌面版（iPad、安卓平板等），因为：
- 屏幕较大，可充分展示表格和完整功能
- 操作不受手持限制
- 管理员通常用平板代替电脑

---

## 技术细节

### 设备检测算法
```javascript
// 前端检测（index_dashboard.html）
const userAgent = navigator.userAgent.toLowerCase();
const mobileKeywords = ['android', 'iphone', 'ipod', ..., 'mobile'];
const isMobile = mobileKeywords.some(k => userAgent.includes(k));
const isTablet = userAgent.includes('ipad') || userAgent.includes('tablet');
if (isMobile && !isTablet) { /* 显示提示 */ }
```

### 后端检测（Python）
```python
def is_mobile_device(request):
    user_agent = request.META.get('HTTP_USER_AGENT', '').lower()
    mobile_keywords = ['android', 'iphone', 'mobile', ...]
    return any(keyword in user_agent for keyword in mobile_keywords)
```

### 重定向流程
```
请求 /admin/ → DeviceAutoRedirectMiddleware.__call__()
            ↓
检查User-Agent是否为手机 (is_mobile_device == True)
            ↓ 是
检查是否有 force_desktop=1 参数
            ↓ 没有
HTTP重定向 301 → /mobile/add/
```

---

## 文件清单

修改/创建的文件：

| 文件 | 类型 | 说明 |
|-----|------|------|
| [`core/device_detector.py`](core/device_detector.py) | 新建 | 设备检测工具模块 |
| [`core/middleware.py`](core/middleware.py) | 修改 | 添加DeviceAutoRedirectMiddleware |
| [`heritage_system/settings.py`](heritage_system/settings.py) | 修改 | 添加中间件配置 |
| [`templates/admin/index_dashboard.html`](templates/admin/index_dashboard.html) | 修改 | 添加前端检测脚本和提示 |
| [`templates/admin/inspection_mobile.html`](templates/admin/inspection_mobile.html) | 修改 | 添加桌面版链接 |
| [`templates/admin/inspection_mobile_list.html`](templates/admin/inspection_mobile_list.html) | 修改 | 添加桌面版链接 |

---

## 总结

✨ **核心特性：**
- ✅ 自动检测设备类型（手机/平板/桌面）
- ✅ 手机用户自动跳转到优化界面
- ✅ 支持强制切换桌面版（?force_desktop=1）
- ✅ 前端客户端识别，显示友好提示
- ✅ 无需用户手动选择，完全自动化
- ✅ 向后兼容，现有功能不受影响

🚀 **效果：**
手机用户打开系统后，自动进入适配手机屏幕的界面，无需任何手动操作，提升用户体验。
