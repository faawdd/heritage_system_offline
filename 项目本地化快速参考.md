# 快速参考指南 - 项目本地化现状速查表

## 📌 一页纸总结

### 国际化配置状态

| 配置项 | 状态 | 说明 |
|--------|------|------|
| `LANGUAGE_CODE` | ✅ | zh-hans（简体中文） |
| `TIME_ZONE` | ✅ | Asia/Shanghai |
| `USE_I18N` | ✅ | 已启用 |
| `USE_TZ` | ✅ | 已启用 |
| **locale/ 目录** | ❌ | **缺失** |
| **Django humanize** | ❌ | **未启用** |
| **统一日期格式** | ❌ | **混乱** |

### 模板文件简表

```
后台页面：14 个（主要功能完整）
  └─ 仪表板：3 个（首页、文物统计、坎儿井）
  └─ 地图页：2 个（分布图、KML 检查）
  └─ 管理页：5 个（KML、巡查、表单等）
  └─ 详情页：2 个（文物、检查）
  └─ 工具页：2 个（转换、导出）

公开页面：2 个（App 宣传）
```

### 字体配置现状

**使用字体族**：
```
主系统：Microsoft YaHei / SimSun / STHeiti
macOS：-apple-system
Web：Segoe UI
移动：Roboto
```

**问题**：不同模板配置不同，需要统一

### 日期格式混乱示例

```django
❌ 模板 A: {{ date|date:'Y-m-d H:i:s' }}        → 2026-04-11 14:30:45
❌ 模板 B: {{ date|date:"Y年m月d日 H:i" }}     → 2026年4月11日 14:30

✅ 应统一为：{{ date|date:'Y年m月d日 H:i' }}
```

---

## 🚀 快速行动计划

### 第 1 步：启用 humanize（5 分钟）

```python
# heritage_system/settings.py
INSTALLED_APPS = [
    # ...
    'django.contrib.humanize',  # ← 添加此行
]
```

### 第 2 步：统一日期格式（30 分钟）

**找出所有日期过滤器**：
```bash
grep -r "date:" templates/
```

**替换模式**：
```django
{# 旧 #}
|date:'Y-m-d H:i:s'
|date:"Y年m月d日"
|date:'Y-m-d'

{# 新 #}
|date:'Y年m月d日 H:i'
|date:'Y年m月d日 H:i'
|date:'Y年m月d日'
```

### 第 3 步：创建 CSS 变量文件（20 分钟）

**新建** `static/admin/css/global-vars.css`：

```css
:root {
    /* 字体 */
    --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI',
                 'Microsoft YaHei', 'SimSun', sans-serif;
    --font-mono: 'Courier New', 'Microsoft YaHei', monospace;
    
    /* 颜色 */
    --primary: #409EFF;
    --success: #67C26A;
    --warning: #E6A23C;
    --danger: #F56C6C;
    --info: #909399;
    
    /* 尺寸 */
    --spacing-xs: 4px;
    --spacing-sm: 8px;
    --spacing-md: 16px;
    --spacing-lg: 24px;
}

body {
    font-family: var(--font-sans);
}
```

---

## 📋 模板维护速查表

### 按功能分类

| 功能分类 | 模板文件 | 关键标签 |
|---------|---------|--------|
| **统计展示** | heritage_dashboard.html | Chart.js 图表 |
| **地图交互** | heritage_map.html | Leaflet 地图库 |
| **数据表格** | *_list.html | Django 原生表格 |
| **详情展示** | heritage_detail.html | 网格布局 |
| **表单输入** | inspection_mobile.html | 移动优化表单 |
| **工具页** | ovkml_converter.html | 文件上传处理 |

### 模板文件大小和复杂度

```
小型（<100 行）：
  - base_site.html
  - export_doc.html

中型（100-500 行）：
  - heritage_detail.html
  - kml_overlay_check.html
  - kanerjing_import_check.html

大型（>500 行）：
  - heritage_dashboard.html（统计逻辑复杂）
  - inspection_mobile.html（表单功能复杂）
  - app_showcase.html（前端交互丰富）
```

---

## 🎯 改进点优先级排序

### 优先级 1（立即做，1-2 天）
- [ ] 启用 humanize app
- [ ] 统一日期格式（全站搜索替换）
- [ ] 创建全局 CSS 变量文件

### 优先级 2（近期做，1-2 周）
- [ ] 统一字体堆栈
- [ ] 创建自定义模板过滤器
- [ ] 更新 admin base template

### 优先级 3（计划中，1 个月）
- [ ] 建立 locale 翻译结构
- [ ] 无障碍审计（WCAG 2.1）
- [ ] 移动端适配优化

---

## 🔧 常用命令速查

```bash
# 查找所有日期过滤器
grep -rn "date:" templates/

# 查找包含特定字符串的模板
grep -rn "inspect_time" templates/

# 统计所有 HTML 文件
find templates -name "*.html" | wc -l

# 生成翻译文件（未来使用）
python manage.py makemessages -l zh_Hans

# 编译翻译（未来使用）
python manage.py compilemessages

# 检查静态文件收集
python manage.py collectstatic --noinput
```

---

## 📞 常见问题解答

### Q1：为什么 Django Admin 后台仍显示英文？
**A**：Django 内置界面翻译需要生成 .mo 文件，但项目尚未建立 locale 目录。

### Q2：humanize 标签和 date 过滤器有什么区别？
**A**：
- `date` 过滤器：固定格式输出（如 "2026-04-11"）
- `humanize` 标签：友好化显示（如 "2 天前"）

### Q3：为什么不同模板的字体不一样？
**A**：各模板独立开发，缺乏统一的设计系统。建议在 base template 中定义。

### Q4：移动端表格如何优化？
**A**：可以使用卡片布局替代表格，或实现横向滚动条，同时优化列宽。

---

## 📚 关键代码片段库

### 启用 humanize（完整版）

```python
# settings.py
INSTALLED_APPS = [
    'core',
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django.contrib.humanize',  # ← 添加这行
    'import_export',
    'rest_framework',
    'rest_framework_simplejwt',
    'corsheaders',
]

# 并在模板中使用
{% load humanize %}

{{ object_list|length|intcomma }}  {# 显示为 1,234 形式 #}
{{ create_date|naturaldate }}      {# 显示为 "2 days ago" #}
{{ create_date|naturaltime }}      {# 显示为 "2 hours, 30 minutes ago" #}
```

### 统一日期过滤器（全局替换方案）

```bash
# 使用 sed 进行全局替换（备份原文件）
cp -r templates templates.backup

# 替换所有英文日期格式
find templates -name "*.html" -exec sed -i '' \
  "s/|date:'Y-m-d H:i:s'/|date:'Y年m月d日 H:i'/g" {} \;

# 替换其他日期格式
find templates -name "*.html" -exec sed -i '' \
  "s/|date:\"Y-m-d\"/|date:'Y年m月d日'/g" {} \;
```

### 自定义时间过滤器（应用到 core app）

```python
# core/templatetags/zh_filters.py
from django import template
from django.utils.timezone import localtime

register = template.Library()

@register.filter
def zh_datetime(dt, format_str='Y年m月d日 H:i'):
    """中文日期时间过滤器"""
    if not dt:
        return '-'
    local_dt = localtime(dt)
    return local_dt.strftime(format_str)

# 使用方式：
# {% load zh_filters %}
# {{ some_date|zh_datetime }}
```

---

## ✨ 最后的话

**当前状态评价**：
- ✅ 框架完善，有本地化意识
- ❌ 执行不完整，翻译文件缺失
- ⚠️ 细节不一致，影响用户体验

**改进投入产出比**：
- **小投入**（2-3 天）→ **大提升**（整体专业度 提升 30%）

**推荐执行顺序**：
1. 启用 humanize（最快见效）
2. 统一日期格式（影响面宽）
3. 创建 CSS 变量（为后续优化奠基）
4. 完善本地化基础设施（长期投资）

---

**文档日期**：2026-04-11  
**版本**：1.0-Quick-Reference  
**最后更新**：系统探索完成
