# 不可移动文物档案详情页 - 使用说明

## 功能概述

已为文物管理系统新增**文物档案详情页**（只读模式），提供以下功能：

### 1. **查看模式专用详情页**
- **目的**：允许用户在查看模式下点击文物名称，进入专门的只读详情页面
- **权限**：仅限登录的工作人员访问（staff_member_required）
- **特点**：完全只读，无编辑、删除等操作选项

### 2. **详情页面包含内容**

#### 📋 基本信息标签页
- 文物名称（高亮显示）
- 四普编号
- 保护级别（带色彩标签）
  - 🔴 全国重点文物保护单位 - 红色
  - 🟠 自治区级文物保护单位 - 橙色
  - 🔵 县级文物保护单位 - 蓝色
  - ⚫ 尚未定级的不可移动文物 - 灰色
- 文物类别
- 管理责任单位/人
- 详细地址
- 现状描述（多行文本）

#### 🌍 坐标与区划标签页
- 经度（Longitude）- 高精度显示
- 纬度（Latitude）- 高精度显示
- **交互式地图预览**
  - 基于 Leaflet 1.9.4
  - 文物位置标记（蓝色圆点）
  - 保护范围多边形（黄色，低透明度）
  - 建控地带多边形（红色虚线，极低透明度）
  - 可缩放、拖拽、点击标记查看详情

#### 🛡️ 两线数据标签页
- **保护范围列表**（如果存在）
  - 表格形式展示所有顶点坐标
  - 序号、经度、纬度三列
  - 精度至小数点后6位

- **建控地带列表**（如果存在）
  - 表格形式展示所有顶点坐标
  - 序号、经度、纬度三列
  - 精度至小数点后6位

#### 📌 巡查记录标签页（如果有记录）
- 最近10条巡查记录
- 显示内容：
  - 巡查时间（年月日时分）
  - 巡查员名称
  - 巡查观察内容
  - 文物状况

### 3. **用户界面特点**

- **标签式导航**：四个标签页面，按需切换查看
- **响应式设计**：自适应不同屏幕尺寸
- **打印友好**：点击"打印"按钮可直接打印该文物详情
- **返回导航**：多处"返回列表"按钮方便用户返回
- **只读提示**：页面顶部蓝色提示框提醒当前为只读模式
- **编辑入口**：页面底部"编辑此文物"按钮快速进入编辑模式

## 使用流程

### 查看模式下查看详情
1. **进入文物列表**
   - 访问 `/admin/core/heritagesite/`
   - 默认为查看模式（灰色说明文字：当前模式：查看）

2. **点击文物名称**
   - 直接点击文物名称超链接
   - 自动打开文物档案详情页：`/admin/heritage/{id}/detail/`

3. **浏览详情信息**
   - 切换标签页查看不同类型的信息
   - 地图标签页可交互式浏览地理位置
   - 两线标签页可查看精确坐标数据

4. **操作选项**
   - **返回列表**：返回文物列表
   - **编辑此文物**：进入编辑模式后编辑文物
   - **打印**：打印当前文物档案

### 编辑模式下查看详情

如需编辑文物信息：
1. 点击列表顶部的"进入编辑模式"按钮
2. 将文物名称链接切换为指向编辑页面
3. 点击文物名称进入编辑页面进行修改
4. 点击详情页底部"编辑此文物"按钮也可进入编辑页面

## URL 路由配置

### 新增路由
```python
path('admin/heritage/<int:pk>/detail/', heritage_detail_view, name='heritage_detail')
```

### 查看模式链接
- 查看模式：`/admin/heritage/{heritage_id}/detail/`
- 编辑模式：`/admin/core/heritagesite/{heritage_id}/change/`

## 前端超链接行为

### change_list.html 中的超链接逻辑

```javascript
// 初始化时（所有超链接）
nameLink.href = '/admin/heritage/' + objectId + '/detail/'

// 切换到编辑模式时
nameLink.href = '/admin/core/heritagesite/' + objectId + '/change/'

// 切换到查看模式时
nameLink.href = '/admin/heritage/' + objectId + '/detail/'
```

### 链接样式
- **查看模式**：灰色，鼠标悬停变蓝色
- **编辑模式**：蓝色加粗，悬停显示下划线

## 后端视图实现

### 数据处理
```python
def heritage_detail_view(request, pk):
    # 获取文物对象
    heritage = get_object_or_404(HeritageSite, pk=pk)
    
    # JSON 解析两线坐标数据
    protection_zone_data = json.loads(heritage.protection_zone) if heritage.protection_zone else []
    control_zone_data = json.loads(heritage.control_zone) if heritage.control_zone else []
    
    # 获取相关巡查记录（最近10条）
    inspection_records = InspectionRecord.objects.filter(
        heritage_site=heritage
    ).order_by('-inspection_date')[:10]
    
    # 渲染模板
    return render(request, 'admin/heritage_detail.html', context)
```

## 页面组件说明

### 地图初始化
- **库**：Leaflet 1.9.4
- **底图**：高德地图/天地图
- **图层**：
  - 文物点标记（circleMarker）
  - 保护范围多边形（polygon）
  - 建控地带多边形（polygon）

### 坐标数据格式
```json
[
  [116.123456, 39.654321],  // [经度, 纬度]
  [116.123457, 39.654322],
  ...
]
```

## 安全性说明

1. **权限保护**：
   - 使用 `@staff_member_required` 装饰器
   - 仅登录的工作人员可访问

2. **数据保护**：
   - 纯只读页面，无表单提交
   - 后端不接受任何修改操作
   - 前端所有交互均为展示类操作

3. **异常处理**：
   - 坐标解析失败时显示为空
   - 无数据时显示友好的"暂无数据"提示
   - 地图加载失败时显示占位符

## 移动端兼容性

- ✅ 响应式设计支持手机/平板访问
- ✅ 地图在移动端可正常交互
- ✅ 标签页在小屏幕上水平可滚动
- ✅ 所有按钮具有足够的点击区域

## 常见问题

### Q: 详情页和编辑页有什么区别？
**A**: 
- **详情页**（本功能）：只读显示，查看模式专用，点击文物名称进入
- **编辑页**：可修改数据，编辑模式专用，提交表单保存更改

### Q: 为什么有些文物没有巡查记录？
**A**: 巡查记录标签页仅在该文物有对应记录时出现。若无记录则不显示该标签。

### Q: 两线坐标如何导入？
**A**: 通过文物编辑页面的 `保护范围坐标集合` 和 `建控地带坐标集合` 字段以 JSON 格式导入。

### Q: 地图为什么没有加载？
**A**: 
- 检查网络连接
- 确认坐标数据有效（经度在 -180~180，纬度在 -90~90）
- 查看浏览器控制台是否有错误日志

## 技术栈

- **后端**：Django 6.0+
- **前端**：Vanilla JavaScript, Leaflet 1.9.4
- **数据格式**：JSON 坐标数据
- **模板引擎**：Django Template Language
- **样式**：CSS3（网格布局、响应式设计）

## 更新日志

### v1.0 (2026-03-05)
- ✅ 实现文物档案详情页
- ✅ 支持四个标签页：基本信息、坐标、两线数据、巡查记录
- ✅ 集成交互式地图显示文物位置和区划
- ✅ 支持打印功能
- ✅ 完整的响应式设计
