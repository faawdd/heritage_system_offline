# 系统版本管理指南

**当前版本**: 1.0  
**最后更新**: 2026年3月5日  
**维护人员**: 系统开发团队

---

## 📌 版本号说明

### 版本号格式
```
版本号: X.Y.Z
- X: 主版本号（大功能更新）
- Y: 次版本号（新功能或改进）
- Z: 修订版本号（bug修复）

示例: 1.0 = 主版本1，次版本0，修订版本0
```

### 当前版本 (1.0)
- **发布日期**: 2026年3月5日
- **版本名称**: 初始版本
- **描述**: 鄯善县文物数字化管理平台正式发布

---

## 🔍 版本号显示位置

系统在多个位置显示当前版本号，方便用户了解系统版本：

### 1. 后台管理首页 - 右下角
- **位置**: 管理后台页面的右下角
- **显示内容**: v1.0
- **样式**: 白色背景，固定位置
- **交互**: 鼠标悬停显示完整版本信息

### 2. 文物数据仪表板 - 右下角
- **位置**: Heritage Dashboard页面的右下角
- **显示内容**: v1.0
- **样式**: 浮动徽章，可点击
- **提示**: 悬停显示 "初始版本 - Released on 2026-03-05"

### 3. 首页底部 (计划中)
- **位置**: 首页footer区域
- **显示内容**: "系统版本: v1.0"
- **样式**: 文本形式，与footer配色一致

---

## 🛠️ 版本号管理

### 文件位置
```
heritage_system/
└─ version.py          # 版本号定义和管理文件
```

### 版本号定义
```python
# 在 heritage_system/version.py 中

__version__ = "1.0"                    # 版本号
__version_name__ = "初始版本"          # 版本名称
__release_date__ = "2026-03-05"        # 发布日期
__author__ = "文物数字化管理团队"       # 开发团队

VERSION = {
    'version': __version__,
    'version_name': __version_name__,
    'release_date': __release_date__,
    'author': __author__,
    'description': '鄯善县文物数字化管理平台'
}
```

---

## 📝 更新版本号 (操作指南)

当需要发布新版本时，按以下步骤操作：

### 步骤1: 修改version.py
```python
# 打开 heritage_system/version.py

# 修改这些值：
__version__ = "1.1"           # 更新版本号（如果是小版本更新）
__version_name__ = "功能改进"  # 更新版本名称
__release_date__ = "2026-03-10" # 更新发布日期
```

### 步骤2: 更新版本历史
在 version.py 的 `VERSION_HISTORY` 列表中添加新版本记录：
```python
VERSION_HISTORY = [
    {
        'version': '1.1',
        'release_date': '2026-03-10',
        'features': [
            '✓ 新增功能1',
            '✓ 新增功能2',
        ],
        'improvements': [
            '✓ 改进1',
            '✓ 改进2',
        ]
    },
    {
        'version': '1.0',
        'release_date': '2026-03-05',
        # ... 原有内容
    }
]
```

### 步骤3: 测试版本显示
1. 重启Django开发服务器
2. 访问后台管理页面，检查右下角版本号是否更新
3. 访问 `http://localhost:8000/api/system-version/` API，验证版本号返回

### 步骤4: 部署到生产环境
1. 提交代码变更
2. 部署到生产服务器
3. 验证生产环境版本号正确

---

## 🔗 版本号集成点

### 1. Django Settings (settings.py)
```python
# 在 heritage_system/settings.py 中导入
from .version import VERSION, get_version_string

# 可在其他模块中使用
SYSTEM_VERSION = VERSION['version']
```

### 2. Context Processors (context_processors.py)
```python
def version_context(request):
    """为模板提供版本号信息"""
    return {
        'APP_VERSION': VERSION['version'],
        'APP_VERSION_STRING': get_version_string(),
        'APP_VERSION_NAME': VERSION['version_name'],
        'APP_RELEASE_DATE': VERSION['release_date'],
    }
```

### 3. 模板中的使用
```html
<!-- 在任何模板中都可以使用以下变量 -->
<p>系统版本: {{ APP_VERSION_STRING }}</p>
<p>发布日期: {{ APP_RELEASE_DATE }}</p>
<p>版本名称: {{ APP_VERSION_NAME }}</p>
```

### 4. API端点 (urls.py)
```python
path('api/system-version/', system_version_api, name='system_version_api')

# API返回格式:
{
    "status": "success",
    "version": {
        "version": "1.0",
        "version_name": "初始版本",
        "release_date": "2026-03-05",
        "author": "文物数字化管理团队",
        "description": "鄯善县文物数字化管理平台"
    },
    "version_history": [...]
}
```

---

## 🌐 版本号API

### 获取版本号信息
**端点**: `/api/system-version/`  
**方法**: GET  
**权限**: 需要登录 (staff_member_required)  
**返回**: JSON格式的版本信息

**示例请求**:
```bash
curl -H "Cookie: sessionid=xxx" http://localhost:8000/api/system-version/
```

**示例返回**:
```json
{
    "status": "success",
    "version": {
        "version": "1.0",
        "version_name": "初始版本",
        "release_date": "2026-03-05",
        "author": "文物数字化管理团队",
        "description": "鄯善县文物数字化管理平台"
    },
    "version_history": [
        {
            "version": "1.0",
            "release_date": "2026-03-05",
            "features": [...],
            "improvements": [...]
        }
    ]
}
```

---

## 📊 版本历史

### v1.0 (2026-03-05) - 初始版本
**主要功能**:
- ✅ 文物档案详情页（只读模式）
- ✅ 文物一张图地图可视化
- ✅ 项目管理功能
- ✅ 坎儿井专项管理
- ✅ 巡查记录管理
- ✅ 手机端适配
- ✅ 权限控制系统
- ✅ 数据导入导出

**主要改进**:
- ✅ 天地图API集成（与文物一张图统一）
- ✅ 地图底图切换（卫星/电子/地形）
- ✅ 两线范围（保护范围和建控地带）叠加显示
- ✅ 自动设备检测和重定向
- ✅ 安全的HTTPS部署

---

## 🔐 版本号相关的安全考虑

### 信息披露
- ✅ 版本号不包含敏感信息
- ✅ API需要用户认证，不向未授权用户显示

### 版本处理
- ✅ 版本号存储在代码中，随部署更新
- ✅ 不能在生产环境直接修改版本号
- ✅ 版本变更需要通过代码审查和测试

---

## 📋 检查清单

发布新版本前，请确保：

- [ ] 版本号在 `version.py` 中已更新
- [ ] 版本历史记录已补充
- [ ] 所有新功能已开发完成
- [ ] 代码已测试无误
- [ ] 文档已更新（本文档或其他相关文档）
- [ ] API版本信息可以正确返回
- [ ] 模板中的版本号显示正确
- [ ] 生产环境部署后验证版本号

---

## 💡 最佳实践

1. **版本号命名规范**
   - 使用语义化版本 (SemVer)
   - 主版本: 不兼容的API变更
   - 次版本: 向后兼容的新功能
   - 修订版本: bug修复

2. **版本发布流程**
   - 更新code中的版本号
   - 添加版本历史记录
   - 提交代码审查
   - 部署到测试环境验证
   - 部署到生产环境
   - 验证版本号显示

3. **版本通信**
   - 通过API提供版本信息
   - 在UI中清晰显示版本
   - 维护版本历史记录
   - 记录各版本的功能和改进

---

## 📞 获取帮助

- **查看当前版本**: 查看后台管理页面右下角
- **获取详细信息**: 访问 `/api/system-version/` API
- **更新版本**: 修改 `heritage_system/version.py`
- **问题反馈**: 与开发团队联系

---

**最后更新**: 2026年3月5日  
**维护团队**: 鄯善县文物数字化管理项目组
