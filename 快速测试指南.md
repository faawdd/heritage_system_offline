<!-- 快速测试指南 - 文物巡查系统修复验证 -->

# 📋 快速测试指南 - 修复验证清单

修复日期: 2026-03-09  
修复内容: 后台500错误 + APP提交功能

---

## 测试 1: 后台管理页面 ✅

### 步骤 1-1: 检查巡查记录列表是否加载

1. 登录后台: `https://yourserver:9081/admin/core/inspectionrecord/`
2. 预期结果: 
   - ✅ 页面正常加载，无500错误
   - ✅ 显示巡查记录列表
   - ✅ 可以看到4个列显示字段：
     - 文物点名称
     - 巡查员
     - 巡查时间
     - 是否正常
     - 位置（经纬度）
     - 照片

### 步骤 1-2: 验证display方法正确处理null值

1. 在列表中查看任意一条记录
2. 检查以下字段:
   - **位置列**: 
     - ✅ 如果有经纬度: 显示 "📍 精度, 纬度"
     - ✅ 如果无位置: 显示 "无位置"  (不报错)
   - **照片列**:
     - ✅ 如果有照片: 显示缩略图
     - ✅ 如果无照片: 显示 "无照片" (不报错)
   - **巡查情况列**:
     - ✅ 正常: 显示绿色"✓ 正常"
     - ✅ 异常: 显示红色"⚠️ 发现问题" + 简述

### 步骤 1-3: 测试异常情况

1. 在后台新增一条巡查记录（不填照片、不填位置）
2. 返回列表查看
3. 验证：
   - ✅ 列表能正常显示，无错误
   - ✅ 位置显示 "无位置"
   - ✅ 照片显示 "无照片"

---

## 测试 2: API 端点验证 ✅

### 步骤 2-1: 获取认证Token

打开终端，运行以下命令获取token:

```bash
# 使用看护员账户登录
curl -X POST "https://yourserver:9081/api/token/" \
  -H "Content-Type: application/json" \
  -d "{
    \"username\": \"your_username\",
    \"password\": \"your_password\"
  }"

# 返回结果示例:
# {
#   "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
#   "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
# }

# 保存 access token 备用
TOKEN="您的access_token"
```

### 步骤 2-2: 测试我的记录列表 API

```bash
# 方式1: 获取第1页，每页20条记录
curl -X GET "https://yourserver:9081/api/inspections/my-records/?page=1&page_size=20" \
  -H "Authorization: Bearer $TOKEN"

# 预期返回:
# {
#   "count": 42,
#   "page": 1,
#   "page_size": 20,
#   "results": [
#     {
#       "id": 1,
#       "site": 2490,
#       "inspector": 20,
#       "inspect_time": "2026-03-09T17:10:10.940310+08:00",
#       "is_normal": true,
#       "issue_details": "西门门柱有破损",
#       "photo": "/media/inspections/2026/03/photo.jpg",
#       "latitude": 39.5,
#       "longitude": 116.5
#     }
#   ]
# }
```

✅ 验证点:
- HTTP 状态码: **200 OK**
- 包含字段: id, site, inspector, inspect_time, is_normal, issue_details, photo, latitude, longitude
- 分页信息: count, page, page_size

### 步骤 2-3: 测试创建新巡查记录（FormData）

```bash
# 模拟APP上传：包含照片和位置信息
curl -X POST "https://yourserver:9081/api/inspections/" \
  -H "Authorization: Bearer $TOKEN" \
  -F "site=2490" \
  -F "is_normal=false" \
  -F "issue_details=发现西门门柱破损，建议及时修缮" \
  -F "latitude=39.5" \
  -F "longitude=116.5" \
  -F "photo=@/path/to/photo.jpg"

# 预期返回: 201 Created
# {
#   "id": 43,
#   "site": 2490,
#   "inspector": 20,
#   "is_normal": false,
#   "issue_details": "发现西门门柱破损，建议及时修缮",
#   "latitude": 39.5,
#   "longitude": 116.5,
#   ...
# }
```

✅ 验证点:
- HTTP 状态码: **201 Created**
- 返回的记录包含所有提交的字段
- 位置信息（经纬度）正确保存

---

## 测试 3: APP同步功能 ✅

### 步骤 3-1: 在APP中提交巡查记录

1. 打开Flutter App
2. 选择一个文物点
3. 拍照（系统会自动添加水印：经纬度、时间、点名称）
4. 输入巡查结果：
   - 是否正常: 选择"异常"
   - 问题描述: 输入 "测试问题：西门有破损"
5. 点击"提交"
6. 预期结果:
   - ✅ 立即上传到后端
   - ✅ 页面显示"提交成功"

### 步骤 3-2: 在后台查看提交的记录

1. 登录后台: `/admin/core/inspectionrecord/`
2. 刷新页面查看最新的记录
3. 验证：
   - ✅ 能看到新提交的记录
   - ✅ 巡查员正确（为APP用户）
   - ✅ 照片能显示
   - ✅ 位置信息显示正确
   - ✅ 问题描述能看到

### 步骤 3-3: 测试同步未上传记录功能

1. 关闭网络，在APP中提交一条巡查记录
   - APP会保存到本地SQLite
   - 显示"待同步"状态
2. 打开网络
3. 点击"同步"按钮
4. 验证：
   - ✅ 调用 `/api/inspections/my-records/` 获取可同步的记录
   - ✅ 逐条上传记录
   - ✅ 同步完成后状态变为"已上传"
5. 回到后台查看
   - ✅ 后台能看到同步的记录
   - ✅ 所有字段完整

---

## 测试 4: 完整端到端流程 ✅

### 场景：完整的巡查流程

**步骤**:
1. 看护员使用APP到达一个文物点
2. 拍照（自动记录GPS位置和时间）
3. 输入巡查结果
4. 提交（APP立即上传）
5. 如果网络中断，APP保存到本地
6. 网络恢复后，点击同步完成上传
7. 后台管理员在后台查看所有巡查记录
8. 可以分类查看、导出、生成报告

**验证清单**:
- [ ] APP能成功提交记录
- [ ] 后台能看到APP提交的记录
- [ ] 位置信息（经纬度）正确保存
- [ ] 照片和问题描述完整
- [ ] 没有400/500错误
- [ ] 同步功能正常工作
- [ ] 后台列表页面不出现500
- [ ] 所有字段显示正确

---

## 快速命令参考

### 1. 检查Django错误日志
```bash
tail -f /var/log/django/error.log
```

### 2. 重启Django服务
```bash
systemctl restart gunicorn
# 或
python manage.py runserver 0.0.0.0:8000
```

### 3. 查看数据库中的巡查记录
```bash
python manage.py dbshell
# SQL:
SELECT id, site_id, inspector_id, is_normal, latitude, longitude, 
       created_at FROM core_inspectionrecord ORDER BY created_at DESC LIMIT 5;
```

### 4. 测试API端点是否生成
```bash
python manage.py shell
# Python:
from rest_framework.routers import DefaultRouter
from core.api_views import InspectionViewSet
router = DefaultRouter()
router.register(r'inspections', InspectionViewSet)
for pattern in router.urls:
    print(pattern.pattern)
```

---

## 常见问题排查

### Q: 后台仍然出现500错误
**A**: 
- 清除Django缓存: `python manage.py clear_cache`
- 检查Python版本: `python --version` (需要 3.9+)
- 查看错误日志: `tail -f /path/to/error.log`
- 重启服务: `systemctl restart gunicorn`

### Q: API 返回404（my-records端点不存在）
**A**:
- 检查ViewSet中是否有my_records方法
- 确认@action装饰器写法: `@action(detail=False, methods=['get'])`
- 重启服务后再试

### Q: APP无法上传照片
**A**:
- 检查文件权限: MEDIA文件夹是否可写
- 检查表单Content-Type: 应为 `multipart/form-data`
- 查看服务器错误日志

### Q: 位置信息没有保存
**A**:
- 检查APP是否发送了 latitude 和 longitude 参数
- 检查Serializer是否包含这两个字段
- 在API中确认参数被接收: 添加日志打印

---

## 修复验证完成确认

以下所有项都✅ 完成时，表示修复成功：

- [ ] 后台巡查记录列表能正常加载（无500错误）
- [ ] 后台能显示位置、照片、问题信息
- [ ] API `/api/inspections/my-records/` 返回200
- [ ] API 返回的数据包含 latitude 和 longitude
- [ ] APP能成功提交巡查记录
- [ ] APP提交的记录能在后台看到
- [ ] 同步功能能正常工作
- [ ] 没有出现400/500/404错误

---

**测试完成时间**: ___________  
**测试人**: ___________  
**测试结果**: ✅ 全部通过 / ❌ 部分失败

如有问题，请提供错误日志和具体的复现步骤。
