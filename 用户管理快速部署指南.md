# 用户管理功能快速部署指南

**部署时间**: 约 5-10 分钟  
**更新日期**: 2026年3月5日

---

## 🚀 快速开始（3步）

### 步骤 1️⃣ : 运行初始化命令

```bash
# 进入项目目录
cd /path/to/heritage_system

# 激活虚拟环境（如果有）
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 运行初始化命令
python manage.py init_user_groups
```

**预期输出**:
```
============================================================
正在初始化用户组和权限配置...

✓ 创建用户组: 超级管理员
✓ 创建用户组: 管理员
✓ 创建用户组: 文物看护员

✓ 用户 wenwu1 已设置为超级管理员

✓ 用户组和权限初始化完成！
============================================================
```

### 步骤 2️⃣ : 创建 wenwu1 账户（如果不存在）

如果 wenwu1 还没有被创建过，执行：

```bash
python manage.py createsuperuser
```

**按提示输入**:
```
Username: wenwu1
Email: admin@example.com
Password: (设定强密码)
Password (again): (重复密码)
```

然后重新运行初始化命令：
```bash
python manage.py init_user_groups
```

### 步骤 3️⃣ : 启动服务验证

```bash
# 开发环境
python manage.py runserver 0.0.0.0:8000

# 生产环境（如果使用Gunicorn）
gunicorn heritage_system.wsgi:application --bind 0.0.0.0:8000
```

访问后台：`http://localhost:8000/admin`

---

## 👤 创建用户（3种方法）

### 方法 A: 通过后台界面（推荐）

1. 以 wenwu1 身份登录后台
2. 导航到 **认证和授权 → 用户**
3. 点击 **添加用户**
4. **必填项**:
   - 用户名（如 `kanerjing01`）
   - 密码（设定初始密码，用户首次登录时修改）
5. **可选项**:
   - 姓名（如 `李某某`）
   - 邮箱 (如 `lmm@example.com`)
6. **权限配置**（重要！）:
   - 在 **用户组** 下拉框中选择：
     - `超级管理员` ← 仅限 wenwu1
     - `管理员` ← 项目经理、协调员
     - `文物看护员` ← 巡查人员
7. 勾选 **是否活跃** 激活账户
8. 点击 **保存**

### 方法 B: 使用管理命令

```bash
# create_inspectors 命令（已有，可修改用途）
python manage.py create_inspectors

# 按提示创建用户及关联信息
```

### 方法 C: Django Shell（开发用）

```bash
python manage.py shell

from django.contrib.auth.models import User, Group

# 创建用户
user = User.objects.create_user(
    username='kanerjing01',
    password='初始密码',
    first_name='李',
    last_name='某某',
    email='lmm@example.com',
    is_staff=True,  # 必须，才能访问后台
)

# 分配用户组
inspector_group = Group.objects.get(name='文物看护员')
user.groups.add(inspector_group)

print(f'✓ 用户 {user.username} 创建成功，已分配到 文物看护员 组')

exit()
```

---

## 🔑 设置超级管理员

系统唯一的超级管理员是 `wenwu1`。设置步骤：

```bash
# 方法 1: 使用 Django 创建超级用户
python manage.py createsuperuser
# 输入 wenwu1 和密码

# 方法 2: 使用我们的初始化命令
python manage.py init_user_groups
# 自动配置 wenwu1 为超级管理员

# 方法 3: Django Shell
python manage.py shell

from django.contrib.auth.models import User, Group

# 获取或创建 wenwu1
user, created = User.objects.get_or_create(
    username='wenwu1',
    defaults={
        'is_staff': True,
        'is_superuser': True,
        'first_name': '文物',
        'email': 'admin@example.com'
    }
)

if created:
    user.set_password('初始密码')
    user.save()

# 确保在超级管理员组中
super_admin = Group.objects.get(name='超级管理员')
user.groups.add(super_admin)

exit()
```

---

## 🎯 常见操作速查

### 为用户分配权限

```
后台 → 用户 → 选择用户 → 修改"用户组" → 保存
```

✅ **修改立即生效**，无需重启

### 添加新看护员

```
后台 → 用户 → 添加用户
→ 输入用户名和密码
→ 在"用户组"中选择"文物看护员"
→ 勾选"是否活跃"
→ 保存
```

### 修改用户组权限

```
后台 → 用户组 → 选择组（如"管理员"）
→ 修改"权限"列表
→ 保存
```

所有该组成员权限自动更新！

### 临时禁用用户账户

```
后台 → 用户 → 选择用户
→ 取消勾选"是否活跃"
→ 保存
```

用户无法登录，但数据保留。

### 删除用户

```
后台 → 用户 → 勾选用户 → 选择"删除" → 确认
```

⚠️ **警告**: 删除用户会删除其巡查记录！

---

## 📊 权限对照表

### 超级管理员 (wenwu1)
```
✅ 所有权限
```

### 管理员
```
✅ 文物档案: 查看
✅ 巡查记录: 新建、修改、查看、删除
✅ 项目管理: 新建、修改、查看、删除
✅ 坐标管理: 新建、修改、查看、删除
✅ 用户管理: 新建、修改、查看
✅ 数据导入导出
```

### 文物看护员
```
✅ 文物档案: 查看
✅ 巡查记录: 新建、修改、查看（仅自己的）
❌ 项目管理: 无法访问
❌ 用户管理: 无法访问
```

---

## 🔍 验证部署

部署完成后，验证以下内容：

- [ ] 以 wenwu1 身份登录成功
- [ ] 后台能看到"用户"、"用户组"、"密码修改记录"三个选项
- [ ] 创建一个"管理员"账户，验证权限正确
- [ ] 创建一个"文物看护员"账户，验证权限正确
- [ ] 修改用户组权限后，相关用户权限立即生效
- [ ] 新建用户自动获得相应权限

---

## ⚠️ 常见问题

**Q: wenwu1 登录失败**  
A: 检查账户是否被激活（后台 → 用户 → 查看"是否活跃"）

**Q: 用户有权限但看不到功能**  
A: 检查用户是否被分配到用户组（后台 → 用户 → 查看"用户组"）

**Q: 修改权限后用户仍无反应**  
A: 让用户重新登录，或检查浏览器缓存

**Q: 怎样添加新的权限类型？**  
A: 在新建 Django 模型时权限自动生成，然后在后台调整用户组权限配置

**Q: 可以有多个超级管理员吗？**  
A: 不建议。设计上 wenwu1 是唯一的超级管理员。其他高权力用户应使用"管理员"组。

---

## 📚 更多资源

- 详细文档: [用户管理与权限控制指南.md](用户管理与权限控制指南.md)
- API文档: 查看 `core/permission_decorators.py`
- Django权限系统: https://docs.djangoproject.com/en/6.0/topics/auth/

---

**版本**: 1.0  
**最后更新**: 2026年3月5日  
**维护**: 鄯善县文物数字化管理项目组
