# ✅ 自动设备检测功能测试清单

## 📋 快速检查要点

### 后端配置检查 ✓ 

- [ ] `core/device_detector.py` 存在
  ```bash
  ls -l core/device_detector.py
  ```

- [ ] `core/middleware.py` 包含 `DeviceAutoRedirectMiddleware`
  ```bash
  grep -n "DeviceAutoRedirectMiddleware" core/middleware.py
  ```

- [ ] `heritage_system/settings.py` 的 MIDDLEWARE 列表包含新中间件
  ```bash
  grep -A2 "DeviceAutoRedirectMiddleware" heritage_system/settings.py
  ```

- [ ] 系统检查无错误
  ```bash
  python manage.py check
  ```
  预期输出：`System check identified no issues (0 silenced).`

---

## 🧪 功能测试

### 测试环境准备

**启动开发服务器：**
```bash
cd heritage_system
python manage.py runserver
```

**访问地址：** `http://localhost:8000/admin/`

---

### ✅ 测试1：手机自动跳转（重要）

#### 步骤1：打开Chrome开发者工具
```
按键盘 F12
或 右键 → 检查
```

#### 步骤2：启用设备模拟
```
1. 点击左上角 📱 设备工具栏图标
2. 选择 "iPhone 12"
3. 重新加载页面 (F5)
```

#### 步骤3：验证自动跳转
```
原访问URL: http://localhost:8000/admin/
预期显示:  http://localhost:8000/mobile/add/
          页面内容显示"📱 添加巡查记录"标题
```

#### 验证项：
- [ ] URL 自动变更为 `/mobile/add/`
- [ ] 显示手机优化界面（大按钮、大文本框）
- [ ] 表单填写界面友好
- [ ] 看不到Django admin管理后台

#### 失败处理：
如果没有跳转：
```bash
# 1. 检查中间件是否启用
grep "DeviceAutoRedirectMiddleware" heritage_system/settings.py

# 2. 重启服务器
# （有时需要重启才能加载新中间件）

# 3. 检查用户是否已登录
# （只有登录后才会执行重定向）
```

---

### ✅ 测试2：桌面版显示（对照组）

#### 步骤：
```
1. 保持Chrome开发者工具打开
2. 点击 "iPhone 12" 旁边的 ❌ 关闭设备模拟
3. 或选择 "Responsive" → 电脑尺寸（1920x1080）
4. 重新加载页面
```

#### 验证项：
- [ ] URL 保持 `/admin/`（不跳转）
- [ ] 显示完整Django管理后台
- [ ] 可看到统计仪表板
- [ ] 显示"文物总数"、"国家级"等卡片

---

### ✅ 测试3：强制桌面版参数

#### 步骤：
```
1. 启用iPhone 12设备模拟
2. 在地址栏输入：
   http://localhost:8000/admin/?force_desktop=1
3. 按Enter
```

#### 验证项：
- [ ] URL 显示 `/admin/?force_desktop=1`
- [ ] **不跳转到** `/mobile/add/`
- [ ] 显示桌面版管理后台（即使在手机模式）
- [ ] 刷新后恢复正常（再次自动跳转）

---

### ✅ 测试4：手机端返回桌面版

#### 步骤：
```
1. 启用iPhone设备模拟
2. 访问 http://localhost:8000/admin/（自动跳转到mobile）
3. 在页面右上角找到"🔗 桌面版"链接
4. 点击链接
```

#### 验证项：
- [ ] URL 变更为 `/mobile/add/?force_desktop=1`
- [ ] 页面切换到桌面版显示
- [ ] 可看到完整的统计数据和图表
- [ ] 返回首页后，再访问 `/admin/` 又自动回到手机版

---

### ✅ 测试5：平板设备检测

#### 步骤：
```
1. 打开Chrome开发者工具 (F12)
2. 点击 📱 设备工具栏
3. 选择 "iPad"
4. 访问 http://localhost:8000/admin/
```

#### 验证项：
- [ ] URL 保持 `/admin/`（**不跳转**）
- [ ] 显示桌面版管理后台
- [ ] 平板被识别为非手机设备

---

### ✅ 测试6：真实手机测试（可选）

#### 准备条件：
- 电脑和手机在同一局域网
- 电脑已启动Django服务器

#### 步骤：
```
1. 获取电脑IP地址：
   - Windows: ipconfig 查看 IPv4
   - Mac/Linux: ifconfig 查看 inet
   例如：192.168.1.100

2. 用手机浏览器访问：
   http://192.168.1.100:8000/admin/

3. 用看护员账户登录
   例如：13565713009 / Heritage2026!
```

#### 验证项：
- [ ] 自动跳转到 `/mobile/add/`
- [ ] 页面在手机屏幕上显示清晰
- [ ] 表单字段尺寸合理（易于触摸）
- [ ] 可正常填写和提交

#### 故障排查：
```bash
# 如果访问失败，检查防火墙
# Windows:
netsh advfirewall firewall add rule name="Django" dir=in action=allow protocol=tcp localport=8000

# 或使用其他可用端口
python manage.py runserver 0.0.0.0:8000
```

---

### ✅ 测试7：首页移动端提示

#### 步骤：
```
1. 启用iPhone设备模拟
2. 访问 http://localhost:8000/admin/home/
3. 如果被跳转，点击"桌面版"返回
4. 或直接访问 http://localhost:8000/admin/home/?force_desktop=1
```

#### 验证项：
- [ ] 在统计卡片上方能看到蓝色提示框
- [ ] 提示文字："📱 检测到移动设备"
- [ ] 提示框右边有"前往手机版"按钮
- [ ] 点击按钮跳转到 `/mobile/add/`

---

## 🔍 日志检查

### 检查是否触发重定向（可选）

启用Django日志，观察请求处理过程：

```python
# 在 heritage_system/settings.py 中添加：
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
        },
    },
    'root': {
        'handlers': ['console'],
        'level': 'DEBUG',
    },
}
```

然后在中间件中添加日志：

```python
# core/middleware.py
import logging
logger = logging.getLogger(__name__)

class DeviceAutoRedirectMiddleware:
    def __call__(self, request):
        if request.user.is_authenticated:
            device = get_device_type(request)
            logger.info(f"用户 {request.user} 设备: {device}")
```

---

## ⚠️ 常见问题排查

### 问题1：设备检测不工作

**症状：** 手机访问时仍显示桌面版，不自动跳转

**排查步骤：**
```bash
# 1. 确认中间件已添加
grep "DeviceAutoRedirectMiddleware" heritage_system/settings.py

# 2. 重建Django缓存
find . -name "*.pyc" -delete
find . -name "__pycache__" -type d -delete

# 3. 重启开发服务器
# Ctrl+C 停止
python manage.py runserver

# 4. 清空浏览器缓存
# Chrome: Ctrl+Shift+Delete
```

### 问题2：所有访问都被重定向

**症状：** 电脑访问 `/admin/` 也跳转到 `/mobile/add/`

**可能原因：**
- User-Agent检测错误
- 浏览器识别有误

**解决方案：**
```python
# 在 core/device_detector.py 中添加调试
def is_mobile_device(request):
    user_agent = request.META.get('HTTP_USER_AGENT', '').lower()
    print(f"User-Agent: {user_agent}")  # 查看实际User-Agent
    # ... 检测逻辑 ...
```

### 问题3：force_desktop 参数不生效

**症状：** 即使添加 `?force_desktop=1` 仍被跳转

**检查项：**
```python
# 确认中间件中有这段代码
if request.GET.get('force_desktop') != '1':
    return redirect('/mobile/add/')
```

---

## 📊 测试结果记录

建议填写此表格记录测试结果：

| 测试项 | 设备 | 预期结果 | 实际结果 | 状态 | 备注 |
|--------|------|--------|--------|-----|------|
| 手机自动跳转 | iPhone | /mobile/add/ | | ✅/❌ | |
| 桌面不跳转 | Chrome | /admin/ | | ✅/❌ | |
| 强制参数 | iPhone | 保持/admin/ | | ✅/❌ | |
| 平板不跳转 | iPad | /admin/ | | ✅/❌ | |
| 首页提示显示 | iPhone | 显示蓝框 | | ✅/❌ | |
| 返回桌面链接 | iPhone | 跳转成功 | | ✅/❌ | |
| 真机测试 | 真实手机 | /mobile/add/ | | ✅/❌ | |

---

## 🎯 完成标准

✅ **所有测试通过时，功能部署完成：**

- [x] 后端配置检查通过
- [ ] 手机自动跳转正常
- [ ] 桌面版独立显示
- [ ] 强制参数功能正确
- [ ] 平板设备识别准确
- [ ] 前端提示显示
- [ ] 返回桌面链接有效

---

## 📞 反馈与支持

如测试中遇到问题，请提供：

1. **设备信息**
   - 设备型号（iPhone 12, iPad, 电脑型号等）
   - 浏览器名称和版本
   
2. **现象描述**
   - 访问URL
   - 预期行为
   - 实际行为
   - 截图或视频

3. **系统日志**
   ```bash
   python manage.py check
   # 粘贴输出信息
   ```

---

**最后更新：2026-03-03**
**版本：1.0**
