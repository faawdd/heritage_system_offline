# 权限管理快速参考指南

> **最后更新**: 2026年3月6日  
> **版本**: 2.0

## 🎯 快速导航

| 需求 | 参考文档 | 命令 |
|------|--------|------|
| **查看权限体系** | [权限管理系统整理说明.md](权限管理系统整理说明.md) | - |
| **初始化权限** | [init_user_groups.py](core/management/commands/init_user_groups.py) | `python manage.py init_user_groups` |
| **添加用户到组** | 下方"用户组管理" | 使用Admin后台 |
| **查看权限代码** | [permission_decorators.py](core/permission_decorators.py) | - |
| **在视图中检查权限** | 下方"在代码中使用" | 使用工具函数 |

---

## 👥 三层权限体系

### 看护员 (文物看护员)
```
权限级别: 🔵 最小化
功能:  ✓ 文物点巡查上报
      ✓ 查看文物档案 (只读)
      ✓ 修改个人资料
```

### 管理员 (管理员)
```
权限级别: 🟡 中等
功能:  ✓ 文物档案管理
      ✓ 巡查记录管理 (全部)
      ✓ 项目建设审批
      ✓ 坐标管理
      ✓ 用户管理 (不能删除)
      ✓ 权限组管理
```

### 超级管理员 (超级管理员)
```
权限级别: 🔴 最高
功能:  ✓ 完全访问全部功能
      ✓ 系统权限管理
      ✓ 数据导入导出
```

---

## 📋 用户组管理

### 方式 1：Admin后台管理（推荐简单场景）

**步骤**：
1. 登录Admin后台
2. 导航到 `鉴权与授权` → `用户`
3. 编辑用户
4. 在"组"中勾选一个用户组
5. 保存

### 方式 2：命令行初始化（推荐首次部署）

```bash
# 初始化权限配置（安全，仅创建不存在的组）
python manage.py init_user_groups

# 完全重置权限配置（谨慎！会删除现有配置）
python manage.py init_user_groups --reset

# 检查当前权限配置
python manage.py init_user_groups --check-only
```

### 方式 3：代码动态分配

```python
from django.contrib.auth.models import User, Group

# 获取用户和组
user = User.objects.get(username='jack1')
inspector_group = Group.objects.get(name='文物看护员')

# 将用户添加到组
user.groups.add(inspector_group)

# 移除用户从组
user.groups.remove(inspector_group)

# 清除所有组并添加新组
user.groups.clear()
user.groups.add(inspector_group)
user.save()
```

---

## 🔐 在代码中使用

### 在视图中使用装饰器

```python
from core.permission_decorators import admin_required, inspector_required

# 仅管理员可访问
@admin_required
def project_list(request):
    projects = ProjectAudit.objects.all()
    return render(request, 'projects/list.html', {'projects': projects})

# 看护员和管理员可访问
@inspector_required
def submit_inspection(request):
    if request.method == 'POST':
        # 处理表单
        pass
    return render(request, 'inspection/form.html')

# 自定义用户组要求
@group_required('管理员', '文物档案员')
def archive_management(request):
    pass
```

### 使用工具函数进行权限检查

```python
from core.permission_decorators import is_admin, is_inspector, get_user_role

def dashboard_view(request):
    user_role = get_user_role(request.user)
    
    if is_admin(request.user):
        # 显示管理员仪表板
        data = get_admin_dashboard(request.user)
    elif is_inspector(request.user):
        # 显示看护员仪表板
        data = get_inspector_dashboard(request.user)
    else:
        # 显示普通用户仪表板
        data = {}
    
    return render(request, 'dashboard.html', {
        'user_role': user_role,
        'data': data,
    })
```

### 在Admin中使用权限控制

```python
from django.contrib import admin
from core.permission_decorators import is_admin

class MyModelAdmin(admin.ModelAdmin):
    def has_add_permission(self, request):
        """仅管理员可以添加"""
        return is_admin(request.user)
    
    def has_delete_permission(self, request, obj=None):
        """仅超级管理员可以删除"""
        return request.user.is_superuser
    
    def get_queryset(self, request):
        """看护员只能查看自己的数据"""
        qs = super().get_queryset(request)
        if request.user.groups.filter(name='文物看护员').exists():
            qs = qs.filter(owner=request.user)
        return qs
```

---

## 🎨 在模板中使用

### 条件显示内容

```html
<!-- 仅管理员显示 -->
{% load permission_decorators %}
{% if user|has_group_filter:"管理员" %}
    <a href="{% url 'admin:core_projectaudit_changelist' %}">项目管理</a>
{% endif %}

<!-- 显示用户角色 -->
<p>您的角色: {{ user|user_role }}</p>

<!-- 使用模板标签 -->
{% can_manage_projects user as can_manage %}
{% if can_manage %}
    <button>+ 新增项目</button>
{% endif %}
```

---

## 🚀 常见操作

### 创建新用户并分配权限

```python
from django.contrib.auth.models import User, Group

# 1. 创建用户
user = User.objects.create_user(
    username='inspector1',
    password='secure_password',
    first_name='张',
    last_name='三'
)

# 2. 分配到看护员组
inspector_group = Group.objects.get(name='文物看护员')
user.groups.add(inspector_group)

# 3. 保存
user.save()
```

### 升级用户权限

```python
from django.contrib.auth.models import User, Group

user = User.objects.get(username='jack1')

# 升级到管理员
admin_group = Group.objects.get(name='管理员')
user.groups.clear()
user.groups.add(admin_group)
user.save()
```

### 降级用户权限

```python
from django.contrib.auth.models import User, Group

user = User.objects.get(username='admin1')

# 降级到看护员
inspector_group = Group.objects.get(name='文物看护员')
user.groups.clear()
user.groups.add(inspector_group)
user.save()
```

### 检查用户权限

```python
from core.permission_decorators import get_permissions_summary

user = User.objects.get(username='jack1')
summary = get_permissions_summary(user)

print(f"用户角色: {summary['role']}")
print(f"用户组: {summary['groups']}")
print(f"是否管理员: {summary['is_admin']}")
```

---

## ⚠️ 常见问题

### Q: 看护员可以看到其他人的巡查记录吗？
**A**: 不能。在Admin后台会被过滤，在 `get_queryset()` 方法中有数据隔离逻辑。

### Q: 如何给现有用户添加权限？
**A**: 
1. **最简单**: 用Admin后台编辑用户，在"组"中选择
2. **快速**: 代码中使用 `user.groups.add(group)`

### Q: 管理员能删除用户吗？
**A**: 不能。只有超级管理员能删除用户。管理员可以添加和编辑用户。

### Q: 如何临时禁用用户？
**A**: 
```python
user = User.objects.get(username='xxx')
user.is_active = False
user.save()
```

###Q: 看护员如何添加巡查记录？
**A**: 
1. Admin后台：`巡查记录` → `新增`（自动锁定为自己）
2. 手机端：`文物点巡查上报` → 填表提交

### Q: 能在模板中检查权限吗?
**A**: 可以！使用过滤器和标签：
```html
{% if user|has_group_filter:"管理员" %}...{% endif %}
{% can_manage_projects user as can_manage %}
```

---

## 📞 技术支持

如有权限相关问题：

1. 查阅详细文档：[权限管理系统整理说明.md](权限管理系统整理说明.md)
2. 查看源代码：
   - 权限装饰器：`core/permission_decorators.py`
   - 权限初始化：`core/management/commands/init_user_groups.py`
   - Admin权限：`core/admin.py` 的各个 `has_*_permission` 方法

3. 运行诊断：
   ```bash
   python manage.py init_user_groups --check-only
   ```

