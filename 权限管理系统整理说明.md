# 文物档案管理系统 - 权限管理整理说明

> **最后更新**: 2026年3月6日  
> **整理人**: 系统管理员  
> **版本**: 2.0

## 📋 目录
1. [权限结构概览](#权限结构概览)
2. [三层权限体系](#三层权限体系)
3. [功能权限清单](#功能权限清单)
4. [实现细节](#实现细节)
5. [权限校验点](#权限校验点)
6. [常见问题](#常见问题)

---

## 权限结构概览

```
用户权限体系
├── Django 超级用户 (is_superuser=True)
│   └── 超级管理员用户组 → 所有系统权限
├── 用户组权限
│   ├── 管理员用户组
│   │   └── 模型级权限 (add/change/view/delete)
│   ├── 文物看护员用户组
│   │   └── 受限权限 (主要是view和add巡查)
│   └── 其他自定义组
└── 视图装饰器权限（补充）
    ├── @staff_member_required (所有后台操作员)
    ├── @admin_required (管理员)
    ├── @inspector_required (看护员)
    └── @group_required (自定义组)
```

---

## 三层权限体系

### 🔵 第一层：文物看护员 (Inspector)
**身份**: 巡查人员  
**职责**: 文物现场巡查和问题上报  
**权限范围**: 最小化权限

| 功能模块 | 权限 | 说明 |
|---------|------|------|
| 文物档案 | View Only | 只能查看文物信息，不能编辑 |
| 巡查上报 | Add/Change/View | 添加新巡查记录 **（仅限自己的记录）** |
| 用户资料 | Change/View | 修改自己的个人资料 |
| 后台访问 | ✅ 受限模式 | 只能访问允许的功能模块 |

**权限规则说明**：
- 新增巡查记录时：`inspector`字段自动锁定为当前用户（不可修改）
- 查看巡查记录时：只能看到自己提交的记录
- 禁止访问：用户管理、项目管理、权限管理等

---

### 🟡 第二层：管理员 (Administrator)
**身份**: 文保科工作人员和领导  
**职责**: 日常系统管理和文物档案维护  
**权限范围**: 大部分功能 (不影响数据安全)

| 功能模块 | 权限 | 说明 |
|---------|------|------|
| 文物档案 | Add/Change/View | 可编辑文物基本信息 |
| 巡查记录 | Add/Change/View/Delete | 查看所有巡查记录，可编辑/删除 |
| 项目管理 | Add/Change/View/Delete | 完整的项目审批流程管理 |
| 坐标管理 | Add/Change/View/Delete | 管理项目建设区坐标 |
| 用户管理 | Add/Change/View | 可以创建用户，不能删除/修改权限 |
| 用户组管理 | Add/Change/View | 可以查看和编辑，不能删除 |
| 用户资料 | Add/Change/View | 管理所有用户的资料 |
| 其他限制 | ❌ 禁止操作 | 不能删除用户，不能修改超级管理员 |

**权限规则说明**：
- 可以查看和管理所有巡查员的记录
- 可以修改文物信息、项目进度等
- 无权访问系统设置、权限管理、日志审计等超级管理员专属功能

---

### 🔴 第三层：超级管理员 (Superuser)
**身份**: 系统最高层级管理员  
**职责**: 系统管理、权限控制、数据安全  
**权限范围**: 完全权限

| 功能模块 | 权限 | 说明 |
|---------|------|------|
| **全部功能** | ✅ Full Access | 具有所有操作权限 |
| 权限管理 | ✅ 完全控制 | 管理用户组和权限 |
| 数据导出导入 | ✅ 完全控制 | 导入导出文物数据 |
| 系统日志 | ✅ 查看审计日志 | 了解用户操作记录 |
| 数据备份恢复 | ✅ 完全控制 | 数据库备份和恢复 |

---

## 功能权限清单

### 🗂️ 不可移动文物档案 (HeritageSite)

| 权限 | 编码 | 看护员 | 管理员 | 超管 |
|------|------|--------|--------|------|
| 查看 | view_heritagesite | ✅ | ✅ | ✅ |
| 新增 | add_heritagesite | ❌ | ✅ | ✅ |
| 编辑 | change_heritagesite | ❌ | ✅ | ✅ |
| 删除 | delete_heritagesite | ❌ | ❌ | ✅ |

### 📋 巡查记录 (InspectionRecord)

| 权限 | 编码 | 看护员 | 管理员 | 超管 |
|------|------|--------|--------|------|
| 查看 | view_inspectionrecord | ✅* | ✅ | ✅ |
| 新增 | add_inspectionrecord | ✅ | ✅ | ✅ |
| 编辑 | change_inspectionrecord | ✅* | ✅ | ✅ |
| 删除 | delete_inspectionrecord | ❌ | ✅ | ✅ |

*看护员仅限自己的记录

### 🏗️ 项目建设审批 (ProjectAudit)

| 权限 | 编码 | 看护员 | 管理员 | 超管 |
|------|------|--------|--------|------|
| 查看 | view_projectauudit | ❌ | ✅ | ✅ |
| 新增 | add_projectaudit | ❌ | ✅ | ✅ |
| 编辑 | change_projectaudit | ❌ | ✅ | ✅ |
| 删除 | delete_projectaudit | ❌ | ✅ | ✅ |

### 📍 坐标数据 (Coordinate)

| 权限 | 编码 | 看护员 | 管理员 | 超管 |
|------|------|--------|--------|------|
| 查看 | view_coordinate | ❌ | ✅ | ✅ |
| 新增 | add_coordinate | ❌ | ✅ | ✅ |
| 编辑 | change_coordinate | ❌ | ✅ | ✅ |
| 删除 | delete_coordinate | ❌ | ✅ | ✅ |

### 👥 用户与群组 (User, Group)

| 权限 | 看护员 | 管理员 | 超管 |
|------|--------|--------|------|
| 管理用户 | ❌ | ✅ | ✅ |
| 管理权限 | ❌ | ❌ | ✅ |
| 删除用户 | ❌ | ❌ | ✅ |

---

## 实现细节

### 1. 权限初始化

**文件**: `core/management/commands/init_user_groups.py`

```bash
# 初始化用户组和权限
python manage.py init_user_groups

# 重置所有用户组（谨慎使用）
python manage.py init_user_groups --reset
```

**配置过程**:
1. ✅ 创建三个用户组（自动创建）
2. ✅ 为每个组分配权限
3. ✅ 配置wenwu1为超级管理员
4. ✅ 显示配置总结

---

### 2. 权限验证装饰器

**文件**: `core/permission_decorators.py`

#### 装饰器列表

| 装饰器 | 用途 | 示例 |
|--------|------|------|
| `@staff_member_required` | 所有后台操作 | 所有后台视图 |
| `@admin_required` | 仅管理员及以上 | 项目管理页面 |
| `@inspector_required` | 仅看护员及以上 | 巡查上报功能 |
| `@super_admin_required` | 仅超级管理员 | 权限管理页面 |
| `@group_required('组1', '组2')` | 自定义组 | 定制页面访问 |

#### 使用示例

```python
from core.permission_decorators import admin_required, inspector_required

@admin_required
def project_list_view(request):
    """只有管理员和超级管理员能访问"""
    pass

@inspector_required
def inspection_add_view(request):
    """看护员、管理员、超级管理员都能访问"""
    pass

@group_required('管理员', '超级管理员')
def sensitive_operation_view(request):
    """仅指定用户组可访问"""
    pass
```

---

### 3. Admin后台权限控制

**文件**: `core/admin.py`

#### 文物看护员的特殊处理

```python
# InspectionAdmin 中的实现

def get_readonly_fields(self, request, obj=None):
    """看护员的inspector字段自动锁定为自己"""
    is_inspector = request.user.groups.filter(name='文物看护员').exists()
    if is_inspector:
        return [..., 'inspector']  # 锁定为只读
    return [...]

def get_queryset(self, request):
    """看护员只能看自己的巡查记录"""
    qs = super().get_queryset(request)
    is_inspector = request.user.groups.filter(name='文物看护员').exists()
    if is_inspector:
        return qs.filter(inspector=request.user)
    return qs

def save_model(self, request, obj, form, change):
    """保存时强制设置inspector"""
    if request.user.groups.filter(name='文物看护员').exists():
        obj.inspector = request.user
    super().save_model(request, obj, form, change)
```

---

### 4. 工具函数

**文件**: `core/permission_decorators.py`

```python
# 检查函数
is_super_admin(user)      # 返回: bool
is_admin(user)            # 返回: bool
is_inspector(user)        # 返回: bool
has_group(user, *groups)  # 返回: bool

# 在模板中使用
{% if user|has_group:"管理员" %}
  <a href="...">管理面板</a>
{% endif %}
```

---

## 权限校验点

### 🔐 核心权限检查位置

1. **Django Admin后台**
   - `InspectionAdmin.get_queryset()` - 数据隔离
   - `InspectionAdmin.get_readonly_fields()` - 字段锁定
   - `InspectionAdmin.save_model()` - 强制赋值

2. **自定义视图**
   - `inspection_mobile_add_view()` - 巡查上报
   - `inspection_mobile_list_view()` - 巡查列表
   - `project_list_view()` - 项目管理

3. **表单级别**
   - `formfield_for_foreignkey()` - 下拉列表过滤
   - 自定义表单验证

---

## 常见问题

### Q1: 看护员如何添加巡查记录？

**A**: 看护员可以通过以下两种方式添加：
1. **Admin后台** → InspectionRecord → 新增
   - `inspector`字段自动设为自己，不可修改
2. **手机端界面** → 文物点巡查上报
   - 直接填写现场照片、问题描述等

### Q2: 管理员能看到所有巡查员的数据吗？

**A**: 是的，管理员有以下权限：
- ✅ 查看所有巡查记录（包括其他人的）
- ✅ 编辑和删除任何巡查记录
- ✅ 导出巡查数据

但看护员只能：
- ✅ 查看自己的记录
- ❌ 看不到其他人的记录

### Q3: 如何给用户分配权限？

**A**: 三种方法：

1. **Admin后台手动分配** （推荐用于单个用户）
   ```
   管理后台 → 用户 → 编辑用户 → 选择"组"
   ```

2. **批量配置** （推荐用于初始化）
   ```bash
   python manage.py init_user_groups --reset
   ```

3. **代码动态分配** （特殊场景）
   ```python
   from django.contrib.auth.models import User, Group
   user = User.objects.get(username='xxx')
   admin_group = Group.objects.get(name='管理员')
   user.groups.add(admin_group)
   ```

### Q4: 看护员可以访问文物点详情页吗？

**A**: 不建议。原因：
- 看护员只需要巡查功能
- 详情页面没有针对看护员的特殊显示
- 目前后台访问有`@staff_member_required`限制

如需开放，需要额外的权限控制。

### Q5: 如何修改权限配置？

**A**: 编辑 `core/management/commands/init_user_groups.py`：

```python
groups_config = {
    '文物看护员': {
        'description': '...',
        'permissions': [
            # 这里修改权限编码
            'view_heritagesite',
            'add_inspectionrecord',
            ...
        ],
    },
    ...
}
```

然后运行：
```bash
python manage.py init_user_groups --reset
```

---

## 安全提示

⚠️ **重要建议**

1. **定期检查权限配置**
   - 每次添加新功能时检查权限
   - 禁止直接修改用户的is_superuser字段

2. **数据隔离要求**
   - 看护员严格隔离（只能看自己的记录）
   - 不要在检查中假设用户权限，始终使用装饰器

3. **审计日志**
   - 建议记录所有权限相关的操作
   - 定期检查谁访问了什么数据

4. **权限拆分原则**
   - 权限最小化：只授予必需权限
   - 权限分离：不同用户组职责明确
   - 权限验证：多层防御（装饰器 + Admin + 数据隔离）

---

## 相关文件

- 📄 [权限装饰器](core/permission_decorators.py) - 装饰器实现
- 📄 [用户组初始化](core/management/commands/init_user_groups.py) - 权限配置
- 📄 [Admin配置](core/admin.py) - 后台权限控制
- 📄 [Models权限](core/models.py) - 数据模型

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 2.0 | 2026-03-06 | ✅ 权限体系完整整理 |
| 1.0 | 2026-03-01 | 初始权限配置 |

